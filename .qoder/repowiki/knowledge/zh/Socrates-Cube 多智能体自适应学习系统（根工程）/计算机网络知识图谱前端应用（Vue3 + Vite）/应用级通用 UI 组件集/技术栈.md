Vue 3 `<script setup lang="ts">` + TypeScript 类型化 Props/Emits；样式使用 scoped CSS（含 keyframes / clip-path / backdrop-filter 等现代特性），图标来自 Element Plus 的 `<el-icon>`，布局辅助类沿用 Tailwind 风格（如 `mb-4`、`flex`、`grid-cols-*`）。