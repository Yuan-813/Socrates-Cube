Vue 3 `<script setup lang="ts">` + Composition API；Canvas 2D 用于登录页网络拓扑/知识图谱动画与引导页粒子/代码雨背景；Web Audio API 在 OnboardingView 中尝试加载 `/sounds/page-turn.mp3`，失败则用 Oscillator 合成翻页音效；OAuth 使用浏览器原生 `fetch` 直接访问后端 `/api/v1/auth/oauth/*`。