Vue 3 `<script setup lang="ts">` + TypeScript；ECharts 5（按需注册 CanvasRenderer/RadarChart/LineChart/GaugeChart 及 Title/Tooltip/Legend/Grid 组件）通过 `vue-echarts` 的 `<v-chart>` 绑定 option；样式使用 scoped CSS 配合 Tailwind 类名（如 `space-y-6`）。