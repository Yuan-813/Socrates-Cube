# Phase 2 详细任务安排（5月18日—5月24日）

> 软件杯A3赛题 · Socrates-Cube · Phase 2
> 核心目标：完成 Diagnosis Agent + 真正的 Orchestrator + 知识库入库验证 + 前后端完整联调
> 今日（5/19）起跑，5/24（周日）晚验收

---

## ⚠️ 首要任务：Phase 1 运行时遗留问题（5/19上午必须清账）

Phase 1 的文件层已全绿，但以下运行时问题**尚未确认**，是本周所有开发的地基。B 和 C 在 5/19 上午各自处理，完成后群内截图确认，A 记录结果到 `docs/results/phase1_issues.md`。

| 问题 | 负责人 | 验证方式 | 预期结果 |
|------|--------|----------|----------|
| 星火API真异步流式是否跑通 | B | 运行 `outputs/test_spark_sse.py`，观察终端 | 流式输出文字，无报错 |
| Chroma 是否已入库（≥3条） | B | `python scripts/ingest_docs.py` 后检索 TCP | 返回 ≥3 条结果 |
| 误解库是否已入库（≥3条） | B | 入库后 `retriever_agent.search_misconceptions("TCP")` | 返回 ≥3 条结果 |
| 数据库4张表是否可读写 | C | `python scripts/init_db.py` + 插入一条记录 | 4张表创建，插入成功 |
| 全链路端到端是否跑通 | 三人 | 前端输入→后端→星火API→SSE流式显示 | 前端看到流式文字 |
| config/prompts/ 7个子目录是否空 | B | `ls config/prompts/*/` | 全部有内容文件 |

**如果某项验证失败，立刻在群内说明，不要等到下午。A 收集后决定是否调整当天任务。**

---

## 📅 5月19日（周一）—— 遗留清账 + 各自起跑

### 👨‍💻 A — 5月19日

**上午：主持遗留问题清账 + 更新 phase1_issues.md**

将 B 和 C 的验证截图汇总，填写 `docs/results/phase1_issues.md`：

```markdown
# Phase 1 遗留问题记录（5/19 清账）

## 已验证通过
- [x] 星火API流式输出：✅ 5/19 B验证通过
- [x] Chroma course_docs 入库：✅ 5/19 B运行ingest，XX条
- [x] 误解库入库：✅ XX条
- [x] 数据库4张表：✅ C验证通过
- [x] 全链路端到端：✅ / ❌（如失败，记录错误信息）

## 遗留Bug
（A填写联调中发现的问题，格式：编号、现象、复现步骤、负责人、截止日期）
```

**下午：开始撰写《系统开发说明书》v1 初稿**

文件路径：`docs/architecture/系统开发说明书_v1.md`

今天完成前三章：

```markdown
# 系统开发说明书 v1

## 第一章 架构总览

### 1.1 C4架构图（Level 1）
[用文字描述，Phase 3补充图片]
- 用户层：学生（主），教师（辅）
- 应用层：Vue3前端（Vite + TailwindCSS + ECharts）
- 业务层：FastAPI后端 + 8 Agent协同（Orchestrator调度）
- 数据层：SQLite + ChromaDB(3个Collection) + JSON知识图谱

### 1.2 8 Agent分工说明
（表格：Agent名称 / 职责 / 输入 / 输出 / 调用时机）

### 1.3 三知识库结构
- course_docs：教材原文（谢希仁第8版5章）
- protocol_specs：RFC规范摘要（可选）
- misconceptions：常见误解库（≥20条）

## 第二章 Agent间State传递设计

### 2.1 全局State字典结构
```json
{
  "session_id": "uuid",
  "user_id": "uuid",
  "current_phase": "profiling|learning|diagnosing|simulating|planning",
  "profile": {...},          // 8维画像，Profiler维护
  "retrieval_context": [...], // 本轮检索结果，Retriever写入
  "diagnosis_result": {...},  // 本轮诊断，Diagnosis写入
  "agent_chain": [],          // 本轮调用链，Orchestrator决定
  "sse_queue": []             // SSE待发送事件
}
```

### 2.2 State传递规则
- Orchestrator持有并传递State引用
- 每个Agent只读/写自己负责的字段
- Agent执行完毕后必须写入AgentLog

## 第三章 关键接口定义
（coordinator.py核心方法签名，参考后端总领文档M5章节）
```

**交付检查点**：`系统开发说明书_v1.md` 第一至三章草稿提交 dev，≥800字。

---

### 🧠 B — 5月19日

**上午：Phase 1遗留清账（见首要任务）**

清账完成后，群内截图：
- 星火API流式输出终端截图（不截密钥）
- Chroma 检索结果截图（`course_docs` + `misconceptions`各≥3条）

如果 `data/cleaned/` 为空导致 ingest 失败，用以下最小测试数据垫底（**不提交Git**）：

```bash
# 临时创建测试文档，让Chroma有数据可用
mkdir -p data/cleaned
cat > data/cleaned/ch03_tcp_udp_test.md << 'EOF'
# 第3章 TCP/UDP协议

## TCP三次握手
TCP建立连接需要三次握手：
第一次：客户端发送SYN报文（SYN=1, seq=x），进入SYN-SENT状态。
第二次：服务端收到SYN后，发送SYN-ACK报文（SYN=1, ACK=1, seq=y, ack=x+1），进入SYN-RCVD状态。
第三次：客户端收到SYN-ACK后，发送ACK报文（ACK=1, ack=y+1），双方进入ESTABLISHED状态。

## TCP四次挥手
TCP断开连接需要四次挥手，因为TCP是全双工协议，每个方向的关闭需要独立确认。

## TCP与UDP的核心区别
TCP是面向连接的、可靠的传输协议，提供流量控制和拥塞控制。
UDP是无连接的、不可靠的传输协议，但延迟低，适用于实时通信。
EOF
python scripts/ingest_docs.py
```

**下午：补齐 config/prompts/ 全部7个空目录的 Prompt 文件**

这是本周最高优先级工程任务之一，必须今天完成，否则后续所有Agent开发都会违反"无硬编码"规范。

**`config/prompts/profiler/update_profile.txt`**：

```
你是计算机网络学习诊断专家。
已有学生的8维画像如下：
{current_profile_json}

根据以下新的对话内容，判断哪些维度需要更新，给出增量更新的JSON。
只更新有新证据的维度，其余维度返回null。
不要猜测，只基于对话内容判断。

新对话：
{new_dialogue}

只返回JSON，格式：
{"dimension_name": new_value_or_null, ...}
```

**`config/prompts/diagnosis/surface_error.txt`**：

```
你是计算机网络认知诊断专家。
分析以下学生的回答，识别其中的表面错误。

错误类型枚举：
- concept_confusion：概念混淆（把A误认为B）
- flow_omission：流程遗漏（缺少关键步骤）
- field_misunderstanding：字段理解错误（误解报文头部字段含义）
- layer_misplacement：层次错位（把某协议放错了网络层次）
- reasoning_breakdown：推理断链（逻辑跳跃、因果关系错误）
- calculation_error：计算错误（序列号/确认号计算错误）
- correct：回答正确

学生回答：
{student_answer}

参考知识库内容：
{reference_content}

只返回JSON：
{
  "error_type": "枚举值",
  "error_description": "用一句话描述具体错误",
  "evidence": "引用学生原话中的错误片段",
  "confidence": 0.0-1.0
}
```

**`config/prompts/diagnosis/root_cause.txt`**：

```
你是计算机网络学习诊断专家。
根据以下表面错误和学生画像，分析根本原因（前置知识薄弱点）。

表面错误：{surface_error_json}
学生8维画像：{profile_json}

知识点依赖关系：
{knowledge_graph_context}

分析：这个表面错误背后，最可能是哪1-3个前置知识点没有掌握？

返回JSON：
[
  {
    "knowledge_node_id": "节点ID",
    "knowledge_node_name": "知识点名称",
    "current_mastery": 0.0-1.0,
    "required_mastery": 0.0-1.0,
    "reason": "为什么缺少这个知识点会导致此错误",
    "evidence_from_profile": "画像中的哪个维度支持此判断"
  }
]
```

**`config/prompts/diagnosis/pattern_match.txt`**：

```
你是计算机网络认知诊断专家。
根据以下表面错误和根因，判断属于哪种误解模式。

误解模式枚举：
- 层次穿越型：混淆不同网络层的协议职责
- 流程死记型：背了流程但不理解每步的意义，稍有变体就出错
- 局部正确型：理解了部分，但把局部知识过度泛化
- 术语混淆型：几个相似术语（如SYN/ACK/FIN）混用
- 过度简化型：理解了简化版本，遇到细节就崩溃
- 无明确模式

表面错误：{surface_error_json}
根因分析：{root_causes_json}

返回JSON：
{
  "pattern": "枚举值",
  "pattern_explanation": "为什么属于这种模式",
  "intervention_suggestion": "针对此模式，最有效的教学干预方式"
}
```

**`config/prompts/retriever/search_query.txt`**：

```
将以下学生问题改写为2-3个适合向量检索的查询语句。
要求：
1. 每个查询聚焦一个检索方向
2. 使用教材标准术语，而不是学生的口语化表达
3. 覆盖：概念定义、流程步骤、协议对比三个角度（不一定每个都需要）

学生问题：{user_message}

只返回JSON数组：["查询1", "查询2", "查询3"]
```

**`config/prompts/orchestrator/route.txt`**：

```
你是多智能体学习系统的调度中枢。
根据以下信息，决定本轮应该调用哪个Agent链，以及调用顺序。

当前会话阶段：{current_phase}
学生画像完整度：{profile_completeness}（0-1，低于0.4时优先画像构建）
学生消息：{user_message}
历史轮数：{turn_count}

可用Agent：
- profiler：画像构建/更新（用户前3-5轮优先调用）
- retriever：知识库检索（几乎每次都需要）
- diagnosis：认知诊断（学生有明确错误时调用）
- simulator：协议仿真（用户询问"流程""步骤""过程"时调用）
- generator：资源生成（用户明确要求或诊断后需要补充资源时调用）
- planner：路径规划（用户问"我该先学什么"或完成一轮学习时调用）

返回JSON：
{
  "decided_phase": "阶段名",
  "agent_chain": ["agent1", "agent2"],
  "reasoning": "一句话说明为什么这样调度"
}
```

**`config/prompts/path_planner/plan_path.txt`**：

```
你是个性化学习路径规划专家。
根据学生画像和知识图谱，为学生规划下一步学习路径。

学生8维画像：{profile_json}
已完成知识点：{completed_nodes}
薄弱知识点（由诊断Agent提供）：{weak_nodes}
知识点依赖图：{knowledge_graph_context}

规划原则：
1. 优先修补薄弱前置知识
2. 遵循知识点依赖关系（不能跳过前置）
3. 路径长度3-5个知识点为宜
4. 每个节点必须给出推荐理由

返回JSON：
{
  "path": [
    {
      "node_id": "知识点ID",
      "node_name": "知识点名称",
      "reason": "为什么先学这个（结合学生画像说明）",
      "estimated_difficulty": 1-5,
      "prerequisite_satisfied": true
    }
  ],
  "overall_rationale": "整体路径设计思路"
}
```

**`config/prompts/challenger/challenge.txt`**：

```
你是计算机网络误解对抗检验专家。
根据以下诊断出的误解，设计一个针对性的检验问题，验证学生是否真正纠正了误解。

已诊断的误解：{misconception_description}
正确理解：{correct_understanding}
学生画像（认知风格）：{cognitive_style}

设计要求：
1. 问题要让学生"必须理解才能答对"，不能靠死记硬背通过
2. 根据认知风格调整问题形式：
   - visual：描述一个场景，让学生判断哪张图正确
   - practical：给一个实际抓包情况，让学生分析
   - textual：概念辨析题
   - analogical：用类比判断题

返回JSON：
{
  "question": "检验问题内容",
  "question_type": "scenario|concept|analogy|analysis",
  "correct_answer": "正确答案要点",
  "distractor_explanation": "常见错误答案及为什么错"
}
```

**`config/prompts/simulator/simulate.txt`**（bonus，有时间就写）：

```
你是网络协议仿真脚本生成器。
根据用户询问的协议流程，生成结构化的仿真脚本，供前端Canvas渲染。

协议流程：{protocol_name}（如：TCP三次握手、HTTP请求响应、ARP解析）
仿真精度：{detail_level}（basic|standard|detailed）

返回JSON：
{
  "protocol_name": "协议名称",
  "total_steps": N,
  "nodes": [
    {"id": "client", "label": "客户端", "x": 80, "y": 100},
    {"id": "server", "label": "服务端", "x": 520, "y": 100}
  ],
  "steps": [
    {
      "step": 1,
      "from": "client",
      "to": "server",
      "packet_label": "SYN",
      "packet_color": "#e74c3c",
      "description": "客户端发送SYN报文，seq=x，进入SYN-SENT状态",
      "state_change": {"client": "SYN-SENT", "server": "LISTEN"}
    }
  ]
}
```

**交付检查点**：`config/prompts/` 下8个目录全部有内容文件（至少各1个非空txt），提交dev。

---

### 🎨 C — 5月19日

**上午：Phase 1遗留清账（见首要任务）**

重点验证：
1. `python scripts/init_db.py` 运行，确认4张表创建成功
2. 向数据库插入一条测试 user 记录并查询，确认读写正常

```python
# 临时验证脚本（不提交Git，放outputs/test_db.py）
import sys
sys.path.insert(0, '.')
from src.loopse.db.connection import init_db, SessionLocal
from src.loopse.db.models import User
import uuid

init_db()
db = SessionLocal()
test_user = User(id=str(uuid.uuid4()), username="test_user")
db.add(test_user)
db.commit()
result = db.query(User).filter(User.username == "test_user").first()
print(f"✅ 数据库读写正常，用户ID：{result.id}")
db.close()
```

**下午：开发 `src/loopse/db/repositories.py`**

Phase 1 中 `db/` 目录有 `models.py` 和 `connection.py`，但缺少数据访问封装层。这是 Orchestrator 能写 AgentLog 的前提。

```python
# src/loopse/db/repositories.py
"""数据访问封装层，供Agent层调用，禁止Agent直接操作db"""
from sqlalchemy.orm import Session
from src.loopse.db.models import User, StudentProfile, ChatSession, AgentLog
from src.loopse.db.connection import SessionLocal
import json, uuid
from datetime import datetime
from contextlib import contextmanager

@contextmanager
def get_db_session():
    db = SessionLocal()
    try:
        yield db
        db.commit()
    except Exception:
        db.rollback()
        raise
    finally:
        db.close()


class UserRepository:
    @staticmethod
    def get_or_create(username: str) -> User:
        with get_db_session() as db:
            user = db.query(User).filter(User.username == username).first()
            if not user:
                user = User(id=str(uuid.uuid4()), username=username)
                db.add(user)
                db.flush()
                db.refresh(user)
            return user


class ProfileRepository:
    @staticmethod
    def get(user_id: str) -> dict | None:
        with get_db_session() as db:
            record = db.query(StudentProfile).filter(
                StudentProfile.user_id == user_id
            ).first()
            return json.loads(record.profile_json) if record else None

    @staticmethod
    def upsert(user_id: str, profile: dict):
        with get_db_session() as db:
            record = db.query(StudentProfile).filter(
                StudentProfile.user_id == user_id
            ).first()
            if record:
                record.profile_json = json.dumps(profile, ensure_ascii=False)
                record.update_time = datetime.now()
            else:
                record = StudentProfile(
                    user_id=user_id,
                    profile_json=json.dumps(profile, ensure_ascii=False)
                )
                db.add(record)


class SessionRepository:
    @staticmethod
    def get_or_create(session_id: str, user_id: str) -> ChatSession:
        with get_db_session() as db:
            session = db.query(ChatSession).filter(
                ChatSession.session_id == session_id
            ).first()
            if not session:
                session = ChatSession(
                    session_id=session_id,
                    user_id=user_id,
                    messages="[]"
                )
                db.add(session)
                db.flush()
                db.refresh(session)
            return session

    @staticmethod
    def append_message(session_id: str, role: str, content: str):
        with get_db_session() as db:
            session = db.query(ChatSession).filter(
                ChatSession.session_id == session_id
            ).first()
            if session:
                msgs = json.loads(session.messages or "[]")
                msgs.append({
                    "role": role,
                    "content": content,
                    "timestamp": datetime.now().isoformat()
                })
                session.messages = json.dumps(msgs, ensure_ascii=False)


class AgentLogRepository:
    @staticmethod
    def write(session_id: str, agent_name: str, action: str,
              input_state: dict, output_state: dict, duration_ms: int = 0):
        with get_db_session() as db:
            log = AgentLog(
                log_id=str(uuid.uuid4()),
                session_id=session_id,
                agent_name=agent_name,
                action=action,
                state=json.dumps({
                    "input": input_state,
                    "output": output_state
                }, ensure_ascii=False),
                timestamp=datetime.now(),
                result=json.dumps(output_state, ensure_ascii=False)
            )
            db.add(log)

    @staticmethod
    def get_session_logs(session_id: str) -> list:
        with get_db_session() as db:
            logs = db.query(AgentLog).filter(
                AgentLog.session_id == session_id
            ).order_by(AgentLog.timestamp).all()
            return [
                {
                    "log_id": l.log_id,
                    "agent_name": l.agent_name,
                    "action": l.action,
                    "timestamp": l.timestamp.isoformat()
                }
                for l in logs
            ]
```

**交付检查点**：`repositories.py` 提交dev，可 `import` 无报错，三个 Repository 类均可实例化。

---

## 📅 5月20日（周二）—— B攻Diagnosis Agent，C攻Orchestrator接口层

### 👨‍💻 A — 5月20日

**全天：完成系统开发说明书第四章 + 设计 coordinator.py 接口**

`docs/architecture/系统开发说明书_v1.md` 第四章：

```markdown
## 第四章 Orchestrator 调度机制设计

### 4.1 会话阶段状态机
（5个阶段：profiling / learning / diagnosing / simulating / planning）
描述每个阶段的触发条件、退出条件、默认Agent链

### 4.2 Agent链构建规则
| 场景 | 触发条件 | Agent链 |
|------|---------|---------|
| 首次对话（前3轮）| turn_count < 3 | [profiler, retriever] |
| 概念问答 | 无明显错误，询问定义 | [retriever] |
| 流程问题 | 含"步骤/过程/握手"关键词 | [retriever, simulator] |
| 发现错误 | 回答有错误 | [retriever, diagnosis, profiler] |
| 请求资源 | 含"给我/生成/我想看" | [retriever, generator] |
| 请求路径 | 含"我该先学/学习计划" | [profiler, planner] |

### 4.3 SSE 事件流设计
（各 Agent 开始/结束时发送的事件格式，与 api_schema.yaml 对齐）
```

同时设计 `coordinator.py` 接口骨架（A只写接口，B填实现）：

```python
# src/loopse/agents/coordinator.py
"""
Orchestrator核心调度逻辑（A设计接口，B实现）
"""
from enum import Enum
from typing import AsyncIterator, List
import time, json

class SessionPhase(str, Enum):
    PROFILING = "profiling"
    LEARNING = "learning"
    DIAGNOSING = "diagnosing"
    SIMULATING = "simulating"
    PLANNING = "planning"

class AgentCoordinator:
    """Agent调度协调器，在Orchestrator中使用"""
    
    def _determine_phase(
        self,
        turn_count: int,
        profile_completeness: float,
        user_message: str
    ) -> SessionPhase:
        """根据上下文判断当前阶段（B实现）"""
        raise NotImplementedError
    
    def _build_agent_chain(
        self,
        phase: SessionPhase,
        user_message: str
    ) -> List[str]:
        """构建Agent调用链（B实现）"""
        raise NotImplementedError
    
    async def _write_log(
        self,
        session_id: str,
        agent_name: str,
        action: str,
        input_state: dict,
        output_state: dict,
        duration_ms: int
    ):
        """写入AgentLog（C的repositories.py实现）"""
        from src.loopse.db.repositories import AgentLogRepository
        AgentLogRepository.write(
            session_id, agent_name, action,
            input_state, output_state, duration_ms
        )
```

**交付检查点**：`coordinator.py` 骨架提交dev；系统开发说明书第四章草稿完成。

---

### 🧠 B — 5月20日

**全天：开发 `src/loopse/agents/diagnosis.py`（三层诊断，Phase 2版本）**

基于总领文档后端篇M8的设计，实现诊断Agent v1：

```python
# src/loopse/agents/diagnosis.py
"""
Diagnosis Agent — 三层认知诊断
Layer 1: 表面错误识别（错误类型 + 置信度）
Layer 2: 根因溯源（前置知识薄弱点）
Layer 3: 误解模式匹配（5种模式）
"""
import json, time
from pathlib import Path
from typing import Optional
from src.loopse.core.llm_client import llm_client
from src.loopse.agents.retriever import retriever_agent

class ErrorType:
    CONCEPT_CONFUSION = "concept_confusion"
    FLOW_OMISSION = "flow_omission"
    FIELD_MISUNDERSTANDING = "field_misunderstanding"
    LAYER_MISPLACEMENT = "layer_misplacement"
    REASONING_BREAKDOWN = "reasoning_breakdown"
    CALCULATION_ERROR = "calculation_error"
    CORRECT = "correct"

class DiagnosisAgent:
    PROMPT_DIR = Path("config/prompts/diagnosis")
    
    def __init__(self):
        self.surface_prompt = self._load("surface_error.txt")
        self.root_cause_prompt = self._load("root_cause.txt")
        self.pattern_prompt = self._load("pattern_match.txt")
    
    def _load(self, filename: str) -> str:
        p = self.PROMPT_DIR / filename
        return p.read_text(encoding="utf-8") if p.exists() else ""
    
    def diagnose(
        self,
        student_answer: str,
        reference_content: str,
        profile: Optional[dict] = None,
        knowledge_graph_context: str = ""
    ) -> dict:
        """
        三层诊断主入口
        返回：{surface_error, root_causes, pattern, confidence, raw_answer}
        """
        # Layer 1：表面错误
        surface = self._detect_surface_error(student_answer, reference_content)
        
        if surface.get("error_type") == ErrorType.CORRECT:
            return {
                "is_correct": True,
                "surface_error": surface,
                "root_causes": [],
                "pattern": "无明确模式"
            }
        
        # Layer 2：根因溯源（需要画像）
        root_causes = self._analyze_root_cause(
            surface, profile or {}, knowledge_graph_context
        )
        
        # Layer 3：误解模式匹配
        pattern = self._match_pattern(surface, root_causes)
        
        return {
            "is_correct": False,
            "surface_error": surface,
            "root_causes": root_causes,
            "pattern": pattern,
            "intervention_suggestion": pattern.get("intervention_suggestion", "")
        }
    
    def _detect_surface_error(self, student_answer: str, reference: str) -> dict:
        prompt = self.surface_prompt.replace(
            "{student_answer}", student_answer
        ).replace("{reference_content}", reference)
        
        result = llm_client.chat(user_message=prompt)
        try:
            clean = result.strip().strip("```json").strip("```").strip()
            return json.loads(clean)
        except Exception:
            return {
                "error_type": ErrorType.CONCEPT_CONFUSION,
                "error_description": "无法解析诊断结果",
                "evidence": student_answer[:100],
                "confidence": 0.5
            }
    
    def _analyze_root_cause(
        self, surface: dict, profile: dict, kg_context: str
    ) -> list:
        prompt = self.root_cause_prompt.replace(
            "{surface_error_json}", json.dumps(surface, ensure_ascii=False)
        ).replace(
            "{profile_json}", json.dumps(profile, ensure_ascii=False)
        ).replace(
            "{knowledge_graph_context}", kg_context or "暂无知识图谱数据"
        )
        
        result = llm_client.chat(user_message=prompt)
        try:
            clean = result.strip().strip("```json").strip("```").strip()
            return json.loads(clean)
        except Exception:
            return []
    
    def _match_pattern(self, surface: dict, root_causes: list) -> dict:
        prompt = self.pattern_prompt.replace(
            "{surface_error_json}", json.dumps(surface, ensure_ascii=False)
        ).replace(
            "{root_causes_json}", json.dumps(root_causes, ensure_ascii=False)
        )
        
        result = llm_client.chat(user_message=prompt)
        try:
            clean = result.strip().strip("```json").strip("```").strip()
            return json.loads(clean)
        except Exception:
            return {"pattern": "无明确模式", "pattern_explanation": "", "intervention_suggestion": ""}

diagnosis_agent = DiagnosisAgent()
```

**写单元测试** `tests/unit/test_diagnosis_agent.py`：

```python
from unittest.mock import patch, MagicMock
from src.loopse.agents.diagnosis import DiagnosisAgent, ErrorType

def test_diagnosis_correct_answer():
    """正确答案应返回is_correct=True"""
    agent = DiagnosisAgent()
    with patch.object(agent, '_detect_surface_error', return_value={
        "error_type": ErrorType.CORRECT,
        "error_description": "回答正确",
        "evidence": "",
        "confidence": 0.95
    }):
        result = agent.diagnose("TCP三次握手建立连接", "TCP三次握手...")
        assert result["is_correct"] == True

def test_diagnosis_concept_confusion():
    """概念混淆错误应触发三层诊断"""
    agent = DiagnosisAgent()
    mock_surface = {
        "error_type": ErrorType.CONCEPT_CONFUSION,
        "error_description": "误以为HTTP直接基于IP",
        "evidence": "HTTP直接基于IP协议",
        "confidence": 0.9
    }
    with patch.object(agent, '_detect_surface_error', return_value=mock_surface):
        with patch.object(agent, '_analyze_root_cause', return_value=[]):
            with patch.object(agent, '_match_pattern', return_value={"pattern": "层次穿越型"}):
                result = agent.diagnose("HTTP直接基于IP", "HTTP基于TCP...")
                assert result["is_correct"] == False
                assert result["surface_error"]["error_type"] == ErrorType.CONCEPT_CONFUSION
```

**交付检查点**：`diagnosis.py` 提交dev，单元测试可 `pytest tests/unit/test_diagnosis_agent.py` 通过（mock模式，不需要真实API调用）。

---

### 🎨 C — 5月20日

**全天：完善 FastAPI 路由层，新增 Profile 和 Logs 接口**

目前只有 `health.py` 和 `chat.py`，需补充：

`src/loopse/api/profile.py`：

```python
from fastapi import APIRouter, HTTPException
from src.loopse.db.repositories import ProfileRepository

router = APIRouter(prefix="/api/v1/profile")

@router.get("/{user_id}")
async def get_profile(user_id: str):
    profile = ProfileRepository.get(user_id)
    if not profile:
        return {"user_id": user_id, "profile": None, "message": "尚未建立画像"}
    return {"user_id": user_id, "profile": profile}

@router.post("/{user_id}")
async def update_profile(user_id: str, profile: dict):
    ProfileRepository.upsert(user_id, profile)
    return {"success": True}
```

`src/loopse/api/logs.py`：

```python
from fastapi import APIRouter
from src.loopse.db.repositories import AgentLogRepository

router = APIRouter(prefix="/api/v1/logs")

@router.get("/session/{session_id}")
async def get_session_logs(session_id: str):
    logs = AgentLogRepository.get_session_logs(session_id)
    return {"session_id": session_id, "logs": logs, "total": len(logs)}
```

在 `src/loopse/main.py` 中注册新路由：

```python
from src.loopse.api.profile import router as profile_router
from src.loopse.api.logs import router as logs_router
app.include_router(profile_router)
app.include_router(logs_router)
```

**交付检查点**：
- `GET /api/v1/profile/{user_id}` 返回200
- `GET /api/v1/logs/session/{session_id}` 返回200（logs为空列表）
- 用 `curl` 或 Swagger UI（`/docs`）截图确认

---

## 📅 5月21日（周三）—— B攻Orchestrator核心实现，C攻前端画像联动

### 👨‍💻 A — 5月21日

**全天：绘制系统架构图（文字版C4图）+ 撰写 Agent 通信 State 设计文档**

文件路径：`docs/architecture/Agent通信State设计.md`

```markdown
# Agent 通信 State 设计说明

## 全局 SessionState 结构
（完整字段定义，与coordinator.py对应）

## 各Agent读写权限说明
| Agent | 只读字段 | 读写字段 | 触发条件 |
|-------|---------|---------|---------|
| Orchestrator | - | agent_chain, current_phase | 每轮调度 |
| Profiler | user_message | profile | 前3轮 / 诊断后 |
| Retriever | user_message, profile | retrieval_context | 几乎每轮 |
| Diagnosis | user_message, retrieval_context, profile | diagnosis_result | 发现错误时 |
| ...  |  |  |  |

## SSE 事件发送规范
（每个Agent在什么时机发送什么类型的SSE事件）
```

**交付检查点**：两份文档提交dev，总字数 ≥1000字。

---

### 🧠 B — 5月21日

**全天：实现 Orchestrator Agent（基于 A 设计的 coordinator.py 骨架）**

`src/loopse/agents/orchestrator.py` 完整实现：

```python
# src/loopse/agents/orchestrator.py
"""
Orchestrator Agent — 总调度，实现基于规则+LLM的混合调度
Phase 2版本：规则优先，LLM做兜底判断
"""
import json, time
from pathlib import Path
from typing import AsyncIterator, Optional
from src.loopse.agents.coordinator import AgentCoordinator, SessionPhase
from src.loopse.agents.profiler import profiler_agent
from src.loopse.agents.retriever import retriever_agent
from src.loopse.agents.diagnosis import diagnosis_agent
from src.loopse.core.llm_client import llm_client
from src.loopse.db.repositories import (
    ProfileRepository, SessionRepository, AgentLogRepository
)

ROUTE_PROMPT = Path("config/prompts/orchestrator/route.txt").read_text(encoding="utf-8") \
    if Path("config/prompts/orchestrator/route.txt").exists() else ""

# 触发仿真的关键词
SIMULATION_KEYWORDS = ["步骤", "过程", "握手", "挥手", "流程", "怎么建立", "怎么断开"]
# 触发路径规划的关键词
PLANNING_KEYWORDS = ["先学什么", "学习计划", "学习路径", "我该从", "建议我"]
# 触发资源生成的关键词
RESOURCE_KEYWORDS = ["给我", "生成", "总结", "思维导图", "练习题", "代码示例"]

class OrchestratorAgent(AgentCoordinator):
    
    def _determine_phase(self, turn_count: int, profile_completeness: float,
                         user_message: str) -> SessionPhase:
        if turn_count < 3 or profile_completeness < 0.3:
            return SessionPhase.PROFILING
        if any(kw in user_message for kw in SIMULATION_KEYWORDS):
            return SessionPhase.SIMULATING
        if any(kw in user_message for kw in PLANNING_KEYWORDS):
            return SessionPhase.PLANNING
        return SessionPhase.LEARNING
    
    def _build_agent_chain(self, phase: SessionPhase, user_message: str) -> list:
        chains = {
            SessionPhase.PROFILING: ["profiler", "retriever"],
            SessionPhase.SIMULATING: ["retriever", "simulator"],
            SessionPhase.PLANNING: ["profiler", "planner"],
            SessionPhase.LEARNING: ["retriever"],
            SessionPhase.DIAGNOSING: ["retriever", "diagnosis", "profiler"],
        }
        return chains.get(phase, ["retriever"])
    
    def _calc_profile_completeness(self, profile: Optional[dict]) -> float:
        if not profile:
            return 0.0
        dims = [
            "network_layer_cognition", "protocol_flow_memory",
            "packet_format_understanding", "protocol_relationship",
            "fault_diagnosis_logic", "hands_on_ability",
            "cognitive_style", "misconception_patterns"
        ]
        filled = sum(1 for d in dims if profile.get(d) is not None)
        return filled / len(dims)
    
    async def process_message(
        self,
        session_id: str,
        user_id: str,
        user_message: str,
        turn_count: int = 0
    ) -> AsyncIterator[str]:
        """核心处理入口，返回SSE事件流"""
        start = time.time()
        
        # 获取画像
        profile = ProfileRepository.get(user_id)
        completeness = self._calc_profile_completeness(profile)
        
        # 确定阶段
        phase = self._determine_phase(turn_count, completeness, user_message)
        agent_chain = self._build_agent_chain(phase, user_message)
        
        # 发送调度开始事件
        yield self._sse_event("agent_start", "Orchestrator", {
            "phase": phase,
            "agent_chain": agent_chain,
            "profile_completeness": completeness
        })
        
        # 写调度日志
        await self._write_log(
            session_id, "Orchestrator", "route_message",
            {"user_message": user_message, "turn_count": turn_count,
             "phase": phase, "profile_completeness": completeness},
            {"agent_chain": agent_chain},
            int((time.time() - start) * 1000)
        )
        
        # 执行Agent链
        context = {
            "user_message": user_message,
            "profile": profile or {},
            "retrieval_context": [],
            "diagnosis_result": None
        }
        
        for agent_name in agent_chain:
            yield self._sse_event("agent_start", agent_name, {"status": "running"})
            agent_start = time.time()
            
            if agent_name == "profiler":
                # 画像更新（同步调用，结果存DB）
                pass  # Phase 3补全，此处先发占位事件
                yield self._sse_event("agent_end", "Profiler", {"status": "skipped_phase2"})
                
            elif agent_name == "retriever":
                results = retriever_agent.search_all(user_message)
                context["retrieval_context"] = results.get("knowledge", [])
                yield self._sse_event("tool_call", "Retriever", {
                    "retrieved_count": len(context["retrieval_context"]),
                    "sources": [r.get("metadata", {}).get("source", "未知") 
                               for r in context["retrieval_context"][:3]]
                })
                
            elif agent_name == "diagnosis":
                ref_text = "\n".join([
                    r["content"] for r in context["retrieval_context"][:2]
                ])
                result = diagnosis_agent.diagnose(
                    user_message, ref_text, profile
                )
                context["diagnosis_result"] = result
                yield self._sse_event("diagnosis", "Diagnosis", result)
            
            await self._write_log(
                session_id, agent_name.capitalize(), "execute",
                {"input": context["user_message"]},
                {"status": "done"},
                int((time.time() - agent_start) * 1000)
            )
            yield self._sse_event("agent_end", agent_name, {"status": "done"})
        
        # 生成最终回复（流式）
        system_prompt = self._build_system_prompt(context)
        yield self._sse_event("agent_start", "LLM", {"status": "streaming"})
        
        async for token in llm_client.async_stream_chat(
            user_message=user_message,
            system_prompt=system_prompt
        ):
            yield self._sse_event("token", "Orchestrator", token)
        
        yield self._sse_event("agent_end", "Orchestrator", {"status": "done"})
        yield self._sse_event("done", "Orchestrator", {})
    
    def _build_system_prompt(self, context: dict) -> str:
        refs = "\n".join([
            f"[来源:{r.get('metadata',{}).get('source','未知')}] {r['content']}"
            for r in context["retrieval_context"][:3]
        ])
        diagnosis_info = ""
        if context.get("diagnosis_result") and not context["diagnosis_result"].get("is_correct", True):
            d = context["diagnosis_result"]
            diagnosis_info = f"\n\n⚠️ 检测到认知错误：{d['surface_error'].get('error_description','')}"
        
        return f"""你是计算机网络智能学习教练。
基于以下知识库内容回答，必须引用知识点来源：
{refs}
{diagnosis_info}
要求：回答清晰、结构化，适合本科生水平。如超出知识库范围，请说明"这超出了当前知识库范围"。"""
    
    @staticmethod
    def _sse_event(event: str, agent_name: str, data) -> str:
        import json
        from datetime import datetime
        payload = {
            "event": event,
            "agent_name": agent_name,
            "data": data if isinstance(data, str) else json.dumps(data, ensure_ascii=False),
            "timestamp": datetime.now().isoformat()
        }
        return f"data: {json.dumps(payload, ensure_ascii=False)}\n\n"


orchestrator = OrchestratorAgent()
```

**更新 `src/loopse/api/chat.py` 接入 Orchestrator**：

```python
# 替换原有的直接调用llm_client，改为调用orchestrator
from src.loopse.agents.orchestrator import orchestrator

@router.post("/stream")
async def chat_stream(request: ChatRequest):
    async def sse_generator():
        turn_count = 0  # Phase 3从DB读取历史轮数
        async for event in orchestrator.process_message(
            session_id=request.session_id,
            user_id=request.user_id,
            user_message=request.message,
            turn_count=turn_count
        ):
            yield event
    
    return StreamingResponse(
        sse_generator(),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache", "X-Accel-Buffering": "no"}
    )
```

**交付检查点**：Orchestrator接入后，`/api/v1/chat/stream` 仍然返回SSE流，且能看到 `agent_start/tool_call/token/done` 等事件类型。

---

### 🎨 C — 5月21日

**全天：前端 ProfileRadar 雷达图接通实际数据**

目前 `ProfileRadar.vue` 已存在，今天任务是让它真正从后端拉画像数据，并监听 SSE 中的 `profile_update` 事件实时刷新。

在 `frontend/src/stores/userStore.ts` 中新增画像状态管理：

```typescript
// 在现有userStore.ts中新增
import { ref, computed } from 'vue'
import { defineStore } from 'pinia'
import axios from 'axios'

export const useUserStore = defineStore('user', () => {
  const userId = ref('demo_user')
  const profile = ref<Record<string, number | null>>({
    network_layer_cognition: null,
    protocol_flow_memory: null,
    packet_format_understanding: null,
    protocol_relationship: null,
    fault_diagnosis_logic: null,
    hands_on_ability: null,
  })
  
  const profileCompleteness = computed(() => {
    const values = Object.values(profile.value)
    const filled = values.filter(v => v !== null).length
    return filled / values.length
  })
  
  async function fetchProfile() {
    try {
      const res = await axios.get(`/api/v1/profile/${userId.value}`)
      if (res.data.profile) {
        Object.assign(profile.value, res.data.profile)
      }
    } catch (e) {
      console.warn('画像获取失败，使用默认值')
    }
  }
  
  function updateFromSSE(profileData: Record<string, any>) {
    // SSE profile_update事件触发时调用
    Object.entries(profileData).forEach(([key, value]) => {
      if (key in profile.value && value !== null) {
        (profile.value as any)[key] = value
      }
    })
  }
  
  return { userId, profile, profileCompleteness, fetchProfile, updateFromSSE }
})
```

在 `ProfileRadar.vue` 中接通 ECharts 雷达图：

```vue
<template>
  <div class="bg-white rounded-xl shadow p-4">
    <div class="flex items-center justify-between mb-3">
      <h3 class="font-bold text-gray-700">协议能力画像</h3>
      <span class="text-xs text-gray-400">完整度 {{ Math.round(completeness * 100) }}%</span>
    </div>
    <div ref="chartEl" style="height: 220px;"></div>
    <div v-if="completeness < 0.3" class="text-center text-gray-400 text-xs mt-2">
      继续对话，画像将自动更新...
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, watch } from 'vue'
import * as echarts from 'echarts'
import { useUserStore } from '@/stores/userStore'
import { storeToRefs } from 'pinia'

const userStore = useUserStore()
const { profile, profileCompleteness: completeness } = storeToRefs(userStore)
const chartEl = ref<HTMLElement>()
let chart: echarts.ECharts

const DIMS = [
  { key: 'network_layer_cognition', label: '分层认知' },
  { key: 'protocol_flow_memory', label: '流程记忆' },
  { key: 'packet_format_understanding', label: '报文理解' },
  { key: 'protocol_relationship', label: '协议关系' },
  { key: 'fault_diagnosis_logic', label: '故障推理' },
  { key: 'hands_on_ability', label: '实操能力' },
]

function getOption() {
  const values = DIMS.map(d => (profile.value as any)[d.key] ?? 1)
  return {
    radar: {
      indicator: DIMS.map(d => ({ name: d.label, max: 5 })),
      splitNumber: 5,
      axisName: { color: '#666', fontSize: 11 }
    },
    series: [{
      type: 'radar',
      data: [{
        value: values,
        name: '能力画像',
        areaStyle: { color: 'rgba(59, 130, 246, 0.2)' },
        lineStyle: { color: '#3b82f6' },
        itemStyle: { color: '#3b82f6' }
      }]
    }]
  }
}

onMounted(async () => {
  await userStore.fetchProfile()
  chart = echarts.init(chartEl.value!)
  chart.setOption(getOption())
})

watch(profile, () => {
  chart?.setOption(getOption())
}, { deep: true })
</script>
```

**交付检查点**：雷达图在 `ProfileView.vue` 正常渲染，访问 `/profile` 页面可看到雷达图组件（值为初始值或从DB读取的画像）。

---

## 📅 5月22日（周四）—— 三人联调日（第一次）

**目标：Orchestrator + Diagnosis + 前端 AgentLogPanel 全链路打通**

### 联调步骤（按顺序执行）

**Step 1（B）**：启动后端

```bash
source .venv/bin/activate
uvicorn src.loopse.main:app --reload --port 8000
```

确认 `/docs`（Swagger UI）能访问所有路由。

**Step 2（C）**：启动前端

```bash
cd frontend && npm run dev
```

确认代理配置正确，前端 `/api/v1/health` 不报 CORS 错误。

**Step 3（三人）**：全链路测试用例

| 测试输入 | 期望SSE事件流 | 期望结果 |
|---------|------------|---------|
| "什么是TCP三次握手" | agent_start(Orchestrator) → agent_start(Retriever) → tool_call → token×N → done | 前端流式显示回复，有知识库引用 |
| "HTTP不需要TCP吧" | agent_start → tool_call → diagnosis(error_type=layer_misplacement) → token×N → done | 前端显示诊断结果 + 回复 |

**Step 4（A）**：记录联调结果

在 `docs/results/phase1_issues.md` 新增 "Phase 2 联调记录" 章节，记录发现的Bug（现象、复现步骤、负责人）。

**Step 5（C）**：接通 AgentLogPanel

在 `frontend/src/components/AgentLogPanel.vue` 中，从 `/api/v1/logs/session/{id}` 拉取日志并展示：

```vue
<!-- 在AgentLogPanel.vue中新增：每次对话结束后刷新日志列表 -->
<div v-for="log in logs" :key="log.log_id" class="flex items-center gap-2 py-1 border-b border-gray-100">
  <span class="text-xs font-mono bg-blue-50 text-blue-600 px-2 py-0.5 rounded">{{ log.agent_name }}</span>
  <span class="text-xs text-gray-500">{{ log.action }}</span>
  <span class="text-xs text-gray-300 ml-auto">{{ formatTime(log.timestamp) }}</span>
</div>
```

**交付检查点**：全链路打通，前端能看到流式回复 + Agent调度日志（至少有Orchestrator和Retriever两条日志记录）。

---

## 📅 5月23日（周五）—— Bug修复 + 知识库扩充 + 文档

### 👨‍💻 A — 5月23日

**全天：完善系统开发说明书 + 更新SRS**

- `系统开发说明书_v1.md` 补充第五章：**测试策略**（单元测试 / 集成测试 / 验收用例对应关系）
- `SRS_v1.md` 更新：将 Phase 1 以来新增的功能（Diagnosis三层诊断、Orchestrator调度、AgentLog可视化）补充进需求条目
- 检查并确认所有Prompt文件均已提交，输出"无硬编码"合规清单

**交付检查点**：两份文档更新提交dev；生成 `docs/requirements/prompt_compliance_check.md`（列出所有prompt文件路径，确认无一遗漏）。

---

### 🧠 B — 5月23日

**上午：扩充知识库（course_docs ≥ 30块，misconceptions ≥ 20条）**

目前 `data/cleaned/` 可能只有测试用的临时文件，今天正式入库：

1. 将已收集的教材资料（或手动整理的知识点）放入 `data/cleaned/`，至少覆盖5个章节
2. 如果暂时没有正式资料，手写每章核心内容（每章至少5个知识块，每块150-300字）
3. 扩充 `data/raw/misconceptions.json` 至 ≥20条，覆盖TCP/IP分层、HTTP/HTTPS、路由、TCP流程4类误解
4. 重新运行 `python scripts/ingest_docs.py`，确认入库数量

```bash
# 验证入库结果
python -c "
from src.loopse.kb.vector_store import vector_store
results = vector_store.search('course_docs', 'TCP三次握手', n_results=5)
print(f'course_docs检索结果：{len(results[\"documents\"][0])}条')
results2 = vector_store.search('misconceptions', 'TCP', n_results=5)
print(f'misconceptions检索结果：{len(results2[\"documents\"][0])}条')
"
```

**下午：修复联调中发现的Bug**

根据 5/22 联调记录，优先修复 B 负责的后端 Bug（Diagnosis/Retriever/llm_client相关）。

**交付检查点**：`data/cleaned/` ≥5个章节文件；Chroma `course_docs` ≥30块；误解库 ≥20条；运行截图提交群。

---

### 🎨 C — 5月23日

**上午：修复联调中发现的前端Bug**

根据 5/22 联调记录，修复 C 负责的前端/后端接口 Bug。

**下午：完善前端 ChatView 的 SSE 多事件类型解析**

目前 `useSSE.ts` 只处理 `token` 类型，今天扩展对 `diagnosis`、`tool_call`、`agent_start/end` 的解析，在前端 UI 上有对应显示：

```typescript
// 在 useSSE.ts 或 chatStore.ts 中扩展事件处理
function handleSSEEvent(event: SSEEvent) {
  switch (event.event) {
    case 'token':
      chatStore.appendStreamToken(event.data)
      break
    case 'agent_start':
      chatStore.updateAgentStatus(event.agent_name, 'running')
      break
    case 'agent_end':
      chatStore.updateAgentStatus(event.agent_name, 'done')
      break
    case 'tool_call':
      chatStore.addToolCallRecord(event.agent_name, event.data)
      break
    case 'diagnosis':
      const diagData = JSON.parse(event.data)
      if (!diagData.is_correct) {
        chatStore.setDiagnosisResult(diagData)
        // 触发 DiagnosisPanel 显示
      }
      break
    case 'done':
      chatStore.finalizeStreaming()
      break
  }
}
```

在 `ChatView.vue` 侧边栏或消息下方，当有 `diagnosis` 事件时，显示简单的诊断标签：

```vue
<div v-if="diagnosisResult && !diagnosisResult.is_correct" 
     class="mt-2 p-2 bg-amber-50 border border-amber-200 rounded-lg text-sm">
  <span class="text-amber-600 font-medium">⚠️ 检测到认知错误：</span>
  <span class="text-amber-800">{{ diagnosisResult.surface_error.error_description }}</span>
</div>
```

**交付检查点**：前端 ChatView 能在对话中展示 Agent 运行状态（loading指示器）+ 诊断提示标签，截图提交群。

---

## 📅 5月24日（周六）—— Phase 2 验收日

**上午：补齐遗漏 + 最终自测**

各人确认本人负责的交付物全部完成并提交 dev。

**下午：A 主持验收例会**

---

## ✅ Phase 2 验收清单（5月24日）

### 必须全绿（Hard）

**⬜ Phase 1 遗留已清账**
- [ ] 星火API真流式调用已实测，有截图存档
- [ ] Chroma `course_docs` 已入库 ≥30块（覆盖3个以上章节）
- [ ] Chroma `misconceptions` 已入库 ≥20条
- [ ] 数据库4张表可正常读写，有验证截图
- [ ] `config/prompts/` 8个子目录全部有内容文件（无空目录）
- [ ] `docs/results/phase1_issues.md` 已填写，遗留Bug有记录

**⬜ Orchestrator（真实调度，不是占位符）**
- [ ] 输入 "什么是TCP三次握手"，后端日志能看到 `Orchestrator→Retriever` 的调用链
- [ ] `agent_logs` 表中有本轮调度记录（至少2条log条目）
- [ ] SSE事件流包含 `agent_start / tool_call / token / done` 全部4类事件
- [ ] `/api/v1/logs/session/{id}` 接口返回正确日志列表

**⬜ Diagnosis Agent（三层诊断）**
- [ ] 输入 "HTTP不需要TCP吧，它直接用IP就行"，Diagnosis Agent 能识别出 `layer_misplacement` 错误
- [ ] 返回的 `diagnosis_result` 包含 `surface_error / root_causes / pattern` 三层结构
- [ ] `tests/unit/test_diagnosis_agent.py` 全部用例 `pytest` 通过（mock模式）
- [ ] DiagnosisPanel 在前端能显示诊断标签（错误类型 + 描述）

**⬜ 前端画像联动**
- [ ] ProfileRadar 雷达图正常渲染（有6个维度，初始值正常显示）
- [ ] 从 `/api/v1/profile/{user_id}` 拉取数据后，雷达图数据正确更新
- [ ] AgentLogPanel 能显示从 `/api/v1/logs/session/{id}` 获取的日志条目

**⬜ 工程质量**
- [ ] `src/loopse/db/repositories.py` 已提交，Agent层通过Repository访问DB，无直接ORM操作
- [ ] 所有Prompt通过文件加载，无一条硬编码在Python代码中
- [ ] `.env` 文件未提交Git（`git log --all -- .env` 无记录）
- [ ] 在新终端 `git clone` + 按 README 启动后端无报错

### 争取完成（Soft，不通过不阻断 Phase 3）

- [ ] Profiler Agent 增量更新：对话3轮后，`student_profiles` 表中画像有变化
- [ ] 前端诊断面板 `DiagnosisView.vue` 接通真实数据（非Mock）
- [ ] `coordinator.py` 中 `_determine_phase` 规则已完整实现
- [ ] `docs/architecture/系统开发说明书_v1.md` ≥4章，总字数≥2000字

---

## 📋 Phase 2 总交付物一览

| 文件/功能 | 负责人 | 截止 | 状态 |
|-----------|--------|------|------|
| Phase 1 遗留清账 + phase1_issues.md | B+C+A | 5/19上午 | 最高优先 |
| `config/prompts/` 8个目录内容文件 | B | 5/19下午 | |
| `src/loopse/db/repositories.py` | C | 5/19下午 | |
| `docs/architecture/系统开发说明书_v1.md` | A | 5/19-5/23 | |
| `src/loopse/agents/coordinator.py`（接口骨架） | A | 5/20 | |
| `docs/architecture/Agent通信State设计.md` | A | 5/21 | |
| `src/loopse/agents/diagnosis.py`（三层诊断） | B | 5/20 | |
| `tests/unit/test_diagnosis_agent.py` | B | 5/20 | |
| `src/loopse/agents/orchestrator.py`（真实调度） | B | 5/21 | |
| 更新 `api/chat.py` 接入 Orchestrator | B | 5/21 | |
| `data/cleaned/` ≥5章 + Chroma ≥30块 + 误解库≥20条 | B | 5/23 | |
| `src/loopse/api/profile.py` | C | 5/20 | |
| `src/loopse/api/logs.py` | C | 5/20 | |
| `ProfileRadar.vue` 接通 ECharts + 画像数据 | C | 5/21 | |
| AgentLogPanel 接通 `/api/v1/logs` | C | 5/22 | |
| 前端 SSE 多事件类型解析 + 诊断标签 | C | 5/23 | |
| 全链路联调 + Bug记录 | 三人 | 5/22 | |

**5月24日下午验收清单全绿（Hard部分）= Phase 2通过，Phase 3正式起跑。**


