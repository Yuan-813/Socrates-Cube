# Phase 3 详细任务安排（5月25日—5月31日）

> 软件杯A3赛题 · Socrates-Cube · Phase 3
> 核心目标：跑通"画像—检索—资源生成—路径规划"完整链路，不追求界面美观
> 5/25（周日）起跑，5/31（周六）晚验收

---

## 🔁 Phase 2 通关前提确认（5/25上午，开始任何Phase 3工作前）

Phase 3 建立在 Phase 2 Hard验收全绿的基础上。5/25 上午 A 逐项确认以下状态，**如有未通过项，优先今天补齐，不要带着欠债进 Phase 3 的正式开发**：

| 来自Phase 2 | 状态确认方式 | 如未通过 |
|------------|------------|---------|
| Orchestrator 真实调度（非占位） | 运行后端，输入问题，看 `agent_logs` 表有无记录 | B 今天上午补 |
| Diagnosis 三层诊断可运行 | `pytest tests/unit/test_diagnosis_agent.py` 全绿 | B 今天上午补 |
| Chroma `course_docs` ≥30块 | Python验证脚本检索 | B 今天上午补 |
| `repositories.py` 可 import | `python -c "from src.loopse.db.repositories import AgentLogRepository"` | C 今天上午补 |
| ProfileRadar 雷达图渲染正常 | 打开前端 `/profile` 页面 | C 今天上午补 |

确认完成后，A 在 `docs/results/phase2_issues.md` 记录遗留问题（如有），再正式起跑。

---

## 📅 5月25日（周日）—— 架构对齐日：知识图谱设计 + Phase 3 分工确认

### 👨‍💻 A — 5月25日

**全天：设计知识图谱 JSON + 撰写测试说明书框架**

**任务一：设计 `data/knowledge_graph.json`（今天必须完成，是B本周的核心依赖）**

知识图谱采用有向图结构（DAG），节点 = 知识点，边 = 前置依赖关系。基于谢希仁第8版5个核心章节，今天设计完整图谱：

```json
{
  "version": "1.0",
  "course": "计算机网络（谢希仁第8版）",
  "nodes": [
    {
      "id": "kn_001",
      "name": "计算机网络基本概念",
      "chapter": "第1章",
      "type": "concept",
      "difficulty": 1,
      "estimated_time": 30,
      "keywords": ["网络", "协议", "体系结构"],
      "description": "计算机网络的定义、分类、组成"
    },
    {
      "id": "kn_002",
      "name": "OSI七层参考模型",
      "chapter": "第1章",
      "type": "concept",
      "difficulty": 2,
      "estimated_time": 45,
      "keywords": ["OSI", "七层", "封装", "解封装"],
      "description": "物理层/数据链路层/网络层/传输层/会话层/表示层/应用层"
    },
    {
      "id": "kn_003",
      "name": "TCP/IP四层模型",
      "chapter": "第1章",
      "type": "concept",
      "difficulty": 2,
      "estimated_time": 40,
      "keywords": ["TCP/IP", "四层", "网际层", "传输层"],
      "description": "网络接口层/网际层/传输层/应用层，与OSI对比"
    },
    {
      "id": "kn_004",
      "name": "数据封装与解封装",
      "chapter": "第1章",
      "type": "process",
      "difficulty": 2,
      "estimated_time": 30,
      "keywords": ["封装", "PDU", "帧", "报文", "数据报"],
      "description": "数据在各层的封装格式：流/段/数据报/帧/比特"
    },
    {
      "id": "kn_005",
      "name": "UDP协议基础",
      "chapter": "第5章",
      "type": "protocol",
      "difficulty": 2,
      "estimated_time": 35,
      "keywords": ["UDP", "无连接", "不可靠", "数据报"],
      "description": "UDP首部格式、特点、适用场景"
    },
    {
      "id": "kn_006",
      "name": "TCP协议基础特性",
      "chapter": "第5章",
      "type": "protocol",
      "difficulty": 3,
      "estimated_time": 50,
      "keywords": ["TCP", "面向连接", "可靠", "全双工"],
      "description": "TCP的六个特点：面向连接/可靠/字节流/全双工/流量控制/拥塞控制"
    },
    {
      "id": "kn_007",
      "name": "TCP报文段格式",
      "chapter": "第5章",
      "type": "format",
      "difficulty": 3,
      "estimated_time": 45,
      "keywords": ["首部", "序号", "确认号", "SYN", "ACK", "FIN", "RST"],
      "description": "TCP首部各字段含义：源/目的端口、序号、确认号、控制位"
    },
    {
      "id": "kn_008",
      "name": "TCP三次握手",
      "chapter": "第5章",
      "type": "process",
      "difficulty": 3,
      "estimated_time": 60,
      "keywords": ["三次握手", "SYN", "SYN-ACK", "连接建立", "SYN-SENT", "SYN-RCVD", "ESTABLISHED"],
      "description": "三次握手过程、状态转换、为什么需要三次"
    },
    {
      "id": "kn_009",
      "name": "TCP四次挥手",
      "chapter": "第5章",
      "type": "process",
      "difficulty": 4,
      "estimated_time": 60,
      "keywords": ["四次挥手", "FIN", "TIME_WAIT", "CLOSE_WAIT", "连接释放"],
      "description": "四次挥手过程、TIME_WAIT的作用、为什么需要四次"
    },
    {
      "id": "kn_010",
      "name": "TCP可靠传输机制",
      "chapter": "第5章",
      "type": "mechanism",
      "difficulty": 4,
      "estimated_time": 70,
      "keywords": ["确认", "重传", "RTT", "超时", "序列号", "ARQ"],
      "description": "停止等待协议、连续ARQ协议、选择重传"
    },
    {
      "id": "kn_011",
      "name": "TCP流量控制",
      "chapter": "第5章",
      "type": "mechanism",
      "difficulty": 4,
      "estimated_time": 50,
      "keywords": ["滑动窗口", "rwnd", "接收窗口", "发送窗口"],
      "description": "滑动窗口机制、零窗口通知、持续定时器"
    },
    {
      "id": "kn_012",
      "name": "TCP拥塞控制",
      "chapter": "第5章",
      "type": "mechanism",
      "difficulty": 5,
      "estimated_time": 80,
      "keywords": ["慢启动", "拥塞避免", "快重传", "快恢复", "cwnd", "ssthresh"],
      "description": "四种拥塞控制算法及其相互关系"
    },
    {
      "id": "kn_013",
      "name": "HTTP协议基础",
      "chapter": "第6章",
      "type": "protocol",
      "difficulty": 3,
      "estimated_time": 60,
      "keywords": ["HTTP", "请求", "响应", "状态码", "无状态", "持久连接"],
      "description": "HTTP/1.0/1.1区别、请求响应格式、常见状态码"
    },
    {
      "id": "kn_014",
      "name": "HTTPS与TLS",
      "chapter": "第6章",
      "type": "protocol",
      "difficulty": 4,
      "estimated_time": 70,
      "keywords": ["HTTPS", "TLS", "证书", "加密", "握手", "对称/非对称"],
      "description": "TLS握手过程、证书验证、加密通信原理"
    },
    {
      "id": "kn_015",
      "name": "DNS解析",
      "chapter": "第6章",
      "type": "process",
      "difficulty": 3,
      "estimated_time": 45,
      "keywords": ["DNS", "域名解析", "递归查询", "迭代查询", "缓存", "TTL"],
      "description": "DNS查询过程、递归与迭代、DNS缓存机制"
    },
    {
      "id": "kn_016",
      "name": "IP地址与子网划分",
      "chapter": "第4章",
      "type": "concept",
      "difficulty": 3,
      "estimated_time": 60,
      "keywords": ["IP地址", "子网掩码", "CIDR", "子网划分", "广播地址"],
      "description": "IP地址分类、子网掩码、CIDR无类别划分"
    },
    {
      "id": "kn_017",
      "name": "路由算法基础",
      "chapter": "第4章",
      "type": "algorithm",
      "difficulty": 4,
      "estimated_time": 75,
      "keywords": ["路由表", "距离向量", "链路状态", "RIP", "OSPF"],
      "description": "距离向量路由算法(RIP)与链路状态路由算法(OSPF)对比"
    },
    {
      "id": "kn_018",
      "name": "ARP协议",
      "chapter": "第4章",
      "type": "protocol",
      "difficulty": 2,
      "estimated_time": 35,
      "keywords": ["ARP", "MAC地址", "地址解析", "ARP缓存", "广播"],
      "description": "ARP工作原理、ARP缓存、免费ARP"
    },
    {
      "id": "kn_019",
      "name": "网络性能指标",
      "chapter": "第1章",
      "type": "concept",
      "difficulty": 2,
      "estimated_time": 40,
      "keywords": ["带宽", "吞吐量", "时延", "RTT", "丢包率", "时延带宽积"],
      "description": "七大性能指标：速率/带宽/吞吐量/时延/时延带宽积/RTT/利用率"
    },
    {
      "id": "kn_020",
      "name": "以太网与MAC帧",
      "chapter": "第3章",
      "type": "protocol",
      "difficulty": 3,
      "estimated_time": 50,
      "keywords": ["以太网", "MAC地址", "CSMA/CD", "帧格式", "最短帧"],
      "description": "以太网MAC帧格式、CSMA/CD碰撞检测、最短帧长计算"
    }
  ],
  "edges": [
    {"from": "kn_001", "to": "kn_002", "relation": "prerequisite"},
    {"from": "kn_001", "to": "kn_003", "relation": "prerequisite"},
    {"from": "kn_002", "to": "kn_004", "relation": "prerequisite"},
    {"from": "kn_003", "to": "kn_004", "relation": "prerequisite"},
    {"from": "kn_003", "to": "kn_005", "relation": "prerequisite"},
    {"from": "kn_003", "to": "kn_006", "relation": "prerequisite"},
    {"from": "kn_006", "to": "kn_007", "relation": "prerequisite"},
    {"from": "kn_007", "to": "kn_008", "relation": "prerequisite"},
    {"from": "kn_007", "to": "kn_009", "relation": "prerequisite"},
    {"from": "kn_008", "to": "kn_010", "relation": "prerequisite"},
    {"from": "kn_010", "to": "kn_011", "relation": "prerequisite"},
    {"from": "kn_011", "to": "kn_012", "relation": "prerequisite"},
    {"from": "kn_006", "to": "kn_013", "relation": "prerequisite"},
    {"from": "kn_013", "to": "kn_014", "relation": "prerequisite"},
    {"from": "kn_001", "to": "kn_015", "relation": "prerequisite"},
    {"from": "kn_001", "to": "kn_016", "relation": "prerequisite"},
    {"from": "kn_016", "to": "kn_017", "relation": "prerequisite"},
    {"from": "kn_016", "to": "kn_018", "relation": "prerequisite"},
    {"from": "kn_001", "to": "kn_019", "relation": "related"},
    {"from": "kn_001", "to": "kn_020", "relation": "prerequisite"},
    {"from": "kn_020", "to": "kn_018", "relation": "related"}
  ],
  "chapters": {
    "第1章": ["kn_001", "kn_002", "kn_003", "kn_004", "kn_019"],
    "第3章": ["kn_020"],
    "第4章": ["kn_016", "kn_017", "kn_018"],
    "第5章": ["kn_005", "kn_006", "kn_007", "kn_008", "kn_009", "kn_010", "kn_011", "kn_012"],
    "第6章": ["kn_013", "kn_014", "kn_015"]
  }
}
```

提交路径：`data/knowledge_graph.json`

**任务二：创建《测试说明书》框架**

文件路径：`docs/test/测试说明书_v1.md`

```markdown
# 测试说明书 v1

## 第一章 测试策略

### 1.1 测试层次
| 层次 | 工具 | 覆盖目标 | 负责人 |
|------|------|---------|--------|
| 单元测试 | pytest + unittest.mock | Agent核心逻辑（不依赖真实API） | B |
| 集成测试 | pytest + httpx | FastAPI接口层（真实DB，Mock LLM） | C |
| 端到端测试 | 手动 / Playwright | 全链路业务场景 | 三人联调 |

### 1.2 Mock策略
- LLM调用：全部Mock，避免测试消耗API配额
- 数据库：使用内存SQLite（:memory:）
- Chroma：使用EphemeralClient（不持久化）

## 第二章 核心业务验收用例（Phase 3）

### UC-01：TCP三次握手完整学习流程
**前置**：知识库已入库，用户画像为空
**步骤**：
1. 输入"什么是TCP三次握手"
2. 期望：Retriever检索到相关内容，LLM基于知识库回复
3. 输入一个错误说法"其实两次握手就够了"
4. 期望：Diagnosis识别出flow_omission错误，触发资源生成
5. 查看资源面板：应有讲解文档 + 练习题 + 代码示例
6. 查看路径面板：应推荐"TCP报文格式→三次握手→可靠传输"路径

**通过标准**：
- Agent日志面板显示完整调用链
- 诊断结果包含错误类型 + 根因 + 误解模式
- 3类资源均可正常显示
- 学习路径包含推荐理由

### UC-02：协议层次混淆诊断
**前置**：同UC-01
**步骤**：输入"HTTP直接基于IP传输，不需要TCP"
**期望**：Diagnosis识别layer_misplacement，根因指向kn_003/kn_013

### UC-03：学习路径请求
**步骤**：输入"我该先学什么"
**期望**：Orchestrator路由到Planner，返回拓扑序路径，每节点有推荐理由

## 第三章 测试用例矩阵
（Phase 3-5逐步补充）
```

**交付检查点**：`data/knowledge_graph.json`（20个节点，≥18条边）提交dev；`docs/test/测试说明书_v1.md` 框架提交dev。

---

### 🧠 B — 5月25日

**全天：补齐 `src/loopse/kb/knowledge_graph.py` + 设计 Resource Generator 接口**

**任务一：实现知识图谱管理模块**（依赖A今天产出的JSON）

```python
# src/loopse/kb/knowledge_graph.py
"""
JSON知识图谱管理，基于A设计的data/knowledge_graph.json
Phase 3：只实现路径规划需要的核心方法
Phase 4：可扩展NetworkX支持
"""
import json
from pathlib import Path
from typing import List, Dict, Optional


class KnowledgeGraph:
    def __init__(self, graph_path: str = "data/knowledge_graph.json"):
        self.graph_path = Path(graph_path)
        self.nodes: Dict[str, dict] = {}
        self.edges: List[dict] = []
        self.adj: Dict[str, List[str]] = {}   # node_id -> [后继节点]
        self.rev_adj: Dict[str, List[str]] = {}  # node_id -> [前驱节点（前置依赖）]
        self._load()

    def _load(self):
        if not self.graph_path.exists():
            print(f"⚠️  知识图谱文件不存在：{self.graph_path}，使用空图谱")
            return
        data = json.loads(self.graph_path.read_text(encoding="utf-8"))
        self.nodes = {n["id"]: n for n in data.get("nodes", [])}
        self.edges = data.get("edges", [])
        for e in self.edges:
            self.adj.setdefault(e["from"], []).append(e["to"])
            self.rev_adj.setdefault(e["to"], []).append(e["from"])

    def get_node(self, node_id: str) -> Optional[dict]:
        return self.nodes.get(node_id)

    def get_prerequisites(self, node_id: str) -> List[str]:
        """获取直接前置依赖（一跳）"""
        return self.rev_adj.get(node_id, [])

    def get_all_prerequisites(self, node_id: str) -> List[str]:
        """获取全部前置依赖（深度优先，去重）"""
        visited, result = set(), []
        def dfs(nid):
            for pre in self.rev_adj.get(nid, []):
                if pre not in visited:
                    visited.add(pre)
                    dfs(pre)
                    result.append(pre)
        dfs(node_id)
        return result

    def topological_sort(self, node_ids: List[str]) -> List[str]:
        """对给定节点子集做拓扑排序"""
        node_set = set(node_ids)
        in_degree = {n: 0 for n in node_ids}
        for e in self.edges:
            if e["from"] in node_set and e["to"] in node_set:
                in_degree[e["to"]] += 1
        queue = [n for n, d in in_degree.items() if d == 0]
        result = []
        while queue:
            node = queue.pop(0)
            result.append(node)
            for succ in self.adj.get(node, []):
                if succ in in_degree:
                    in_degree[succ] -= 1
                    if in_degree[succ] == 0:
                        queue.append(succ)
        return result

    def find_weak_prerequisites(
        self, target_node_id: str, profile: dict, threshold: float = 0.6
    ) -> List[dict]:
        """
        找出到达目标节点所需的前置知识中，掌握度低于阈值的节点
        返回：[{"node_id": ..., "node_name": ..., "mastery": ...}]
        """
        all_pres = self.get_all_prerequisites(target_node_id)
        weak = []
        for nid in all_pres:
            node = self.nodes.get(nid)
            if not node:
                continue
            # 从画像中查找该知识点的掌握度（按keyword匹配）
            mastery = self._estimate_mastery(nid, profile)
            if mastery < threshold:
                weak.append({
                    "node_id": nid,
                    "node_name": node["name"],
                    "chapter": node.get("chapter", ""),
                    "mastery": mastery,
                    "estimated_time": node.get("estimated_time", 30)
                })
        return weak

    def _estimate_mastery(self, node_id: str, profile: dict) -> float:
        """从画像估算对某知识点的掌握度（简单映射，Phase 4可精化）"""
        node = self.nodes.get(node_id, {})
        chapter = node.get("chapter", "")
        node_type = node.get("type", "")

        dim_map = {
            "第1章": "network_layer_cognition",
            "第3章": "hands_on_ability",
            "第4章": "protocol_relationship",
            "第5章": "protocol_flow_memory",
            "第6章": "protocol_relationship",
        }
        dim = dim_map.get(chapter, "network_layer_cognition")
        raw = profile.get(dim)
        if raw is None:
            return 0.2  # 未知 → 保守估计较低

        # raw可能是1-5的整数评分，归一化到0-1
        if isinstance(raw, (int, float)) and raw > 1:
            return min(raw / 5.0, 1.0)
        return float(raw) if raw else 0.2

    def get_chapter_nodes(self, chapter: str) -> List[dict]:
        return [n for n in self.nodes.values() if n.get("chapter") == chapter]

    def search_by_keyword(self, keyword: str) -> List[dict]:
        """按关键词搜索知识点"""
        return [
            n for n in self.nodes.values()
            if keyword in n.get("keywords", []) or keyword in n.get("name", "")
        ]


# 全局单例
knowledge_graph = KnowledgeGraph()
```

**任务二：设计 Resource Generator 的Prompt文件**（B写，C不用等）

`config/prompts/resource_generator/generate_doc.txt`：

```
你是计算机网络教学专家，请为以下知识点生成一份教学讲解文档。

知识点：{knowledge_point}
学生认知风格：{cognitive_style}（visual=图解为主/textual=推导为主/practical=实操为主/analogical=类比为主）
诊断到的薄弱点：{weakness_summary}
知识库参考内容：
{reference_content}

生成要求：
1. 标题简洁，正文300-500字
2. 根据认知风格调整侧重：
   - visual：多用"如图所示""流程如下"引导，结构用Markdown列表
   - textual：多用定义→推导→结论的逻辑链
   - practical：多用具体操作步骤和命令示例
   - analogical：用1个日常生活类比开头
3. 结尾必须注明：来源章节 = {source_chapter}
4. 不要编造不在参考内容中的知识

以Markdown格式返回，不要加代码块标记。
```

`config/prompts/resource_generator/generate_exercise.txt`：

```
你是计算机网络习题设计专家，请为以下知识点生成一道针对性练习题。

知识点：{knowledge_point}
已诊断的错误类型：{error_type}（针对此错误设计题目，让学生"必须理解才能答对"）
题型：{exercise_type}（choice=单选/fill_blank=填空/scenario=情景分析）
参考知识库内容：{reference_content}

返回JSON（不要加代码块标记）：
{
  "question": "题目内容",
  "options": [
    {"label": "A", "text": "选项内容", "is_distractor": false},
    {"label": "B", "text": "选项内容", "is_distractor": true},
    {"label": "C", "text": "选项内容", "is_distractor": true},
    {"label": "D", "text": "选项内容", "is_distractor": true}
  ],
  "correct_answer": "A",
  "explanation": "解析：为什么A正确，以及其他选项为什么错（直接针对错误类型{error_type}解释）",
  "targeted_misconception": "本题针对的具体误解描述"
}
```

`config/prompts/resource_generator/generate_code.txt`：

```
你是计算机网络实操专家，请为以下知识点生成一份Python代码实操案例。

知识点：{knowledge_point}
参考内容：{reference_content}
目标：让学生通过运行代码，直观理解 {knowledge_point} 的工作机制

代码要求：
1. 使用Python标准库（socket / struct / scapy按需选用）
2. 代码量50-100行，有详细注释
3. 可以在本地直接运行（不依赖外部服务器）
4. 代码后附：预期输出说明 + 1个思考问题

返回JSON（不要加代码块标记）：
{
  "title": "代码案例标题",
  "language": "python",
  "code": "完整代码（含注释）",
  "explanation": "代码逻辑解释（3-5句）",
  "expected_output": "预期运行输出",
  "wireshark_filter": "推荐的Wireshark过滤器（如：tcp.flags.syn==1）",
  "think_question": "一个让学生思考的问题"
}
```

**交付检查点**：`src/loopse/kb/knowledge_graph.py` 提交dev，可 `import` 无报错；`knowledge_graph.load()` 加载知识图谱返回正确节点数；3个 Resource Generator Prompt 文件提交。

---

### 🎨 C — 5月25日

**全天：新增前端 TypeScript 类型定义 + API客户端（为本周组件开发做准备）**

**任务一：补齐 `frontend/src/types/` 缺失类型**

按前端总领文档，补充以下文件（Phase 1/2 只建了 `sse.ts` 和部分 `index.ts`）：

`frontend/src/types/resource.ts`：（完整按前端总领文档中的类型定义写，包含 `DocResource / ExerciseResource / CodeResource / MindmapResource / ScriptResource`）

`frontend/src/types/path.ts`：（包含 `PathNode / LearningPath / PathNodeStatus / PathNodeType`）

`frontend/src/types/diagnosis.ts`：（包含 `DiagnosisResult / SurfaceError / RootCause / MisconceptionPattern / ErrorType`）

**任务二：新增 API 客户端**

`frontend/src/api/resources.ts`：

```typescript
import { api } from './client'
import type { BaseResource } from '@/types/resource'

export const resourcesApi = {
  getBySession: (sessionId: string) =>
    api.get<BaseResource[]>(`/api/v1/resources`, { params: { session_id: sessionId } }),

  getById: (resourceId: string) =>
    api.get<BaseResource>(`/api/v1/resources/${resourceId}`),
}
```

`frontend/src/api/path.ts`：

```typescript
import { api } from './client'
import type { LearningPath } from '@/types/path'

export const pathApi = {
  getUserPath: (userId: string) =>
    api.get<LearningPath>(`/api/v1/path/${userId}`),

  updateProgress: (userId: string, nodeId: string, status: string) =>
    api.post(`/api/v1/path/${userId}/progress`, { node_id: nodeId, status }),
}
```

**任务三：新建 Pinia stores**

`frontend/src/stores/resourceStore.ts`：

```typescript
import { ref } from 'vue'
import { defineStore } from 'pinia'
import { resourcesApi } from '@/api/resources'
import type { BaseResource } from '@/types/resource'

export const useResourceStore = defineStore('resources', () => {
  const resources = ref<BaseResource[]>([])
  const activeTab = ref<string>('doc')
  const isLoading = ref(false)

  function addResource(resource: BaseResource) {
    // SSE resource事件触发，追加资源
    resources.value.push(resource)
    activeTab.value = resource.resource_type
  }

  function clearSession() {
    resources.value = []
    activeTab.value = 'doc'
  }

  return { resources, activeTab, isLoading, addResource, clearSession }
})
```

`frontend/src/stores/pathStore.ts`：

```typescript
import { ref, computed } from 'vue'
import { defineStore } from 'pinia'
import { pathApi } from '@/api/path'
import type { LearningPath, PathNode } from '@/types/path'

export const usePathStore = defineStore('path', () => {
  const path = ref<LearningPath | null>(null)
  const selectedNode = ref<PathNode | null>(null)

  const completedCount = computed(() =>
    path.value?.nodes.filter(n => n.status === 'completed').length ?? 0
  )

  async function fetchPath(userId: string) {
    try {
      const res = await pathApi.getUserPath(userId)
      path.value = res.data
    } catch (e) {
      console.warn('学习路径获取失败')
    }
  }

  function updateFromSSE(pathData: LearningPath) {
    path.value = pathData
  }

  return { path, selectedNode, completedCount, fetchPath, updateFromSSE }
})
```

**交付检查点**：3个类型文件 + 2个API模块 + 2个store 提交dev，`npm run build` 无类型错误。

---

## 📅 5月26日（周一）—— B攻Resource Generator，C攻资源卡片组件

### 👨‍💻 A — 5月26日

**全天：补充测试说明书 + 编写 Phase 3 全链路验收用例**

在 `docs/test/测试说明书_v1.md` 中补充第二章完整用例矩阵（按 UC-01/02/03 格式，覆盖Phase 3全部验收场景），并新建：

`docs/test/phase3_acceptance_cases.md`：列出5月31日验收时的7个测试场景，每个包含：输入 / 期望SSE事件序列 / 期望前端显示 / 通过标准。

**交付检查点**：验收用例文档提交dev，≥5个业务场景，每场景有明确通过标准。

---

### 🧠 B — 5月26日

**全天：开发 `src/loopse/agents/resource_generator.py`（3类资源）**

```python
# src/loopse/agents/resource_generator.py
"""
Resource Generator Agent — 5类多表征资源生成（Phase 3先实现前3类）
doc: 讲解文档（Markdown）
exercise: 练习题（JSON结构）
code: 代码实操案例（JSON结构）
mindmap / script：Phase 4实现
"""
import json, uuid
from datetime import datetime
from pathlib import Path
from typing import List, Optional
from src.loopse.core.llm_client import llm_client


class ResourceGeneratorAgent:
    PROMPT_DIR = Path("config/prompts/resource_generator")

    def __init__(self):
        self.prompts = {
            "doc": self._load("generate_doc.txt"),
            "exercise": self._load("generate_exercise.txt"),
            "code": self._load("generate_code.txt"),
        }

    def _load(self, filename: str) -> str:
        p = self.PROMPT_DIR / filename
        return p.read_text(encoding="utf-8") if p.exists() else ""

    def generate(
        self,
        knowledge_point: str,
        resource_types: List[str],
        cognitive_style: str = "textual",
        error_type: str = "",
        reference_content: str = "",
        source_chapter: str = "",
        weakness_summary: str = ""
    ) -> List[dict]:
        """
        生成多类型资源
        返回：[{"resource_type": ..., "title": ..., "content": ..., ...}]
        """
        results = []
        for rtype in resource_types:
            generator = getattr(self, f"_generate_{rtype}", None)
            if generator:
                resource = generator(
                    knowledge_point=knowledge_point,
                    cognitive_style=cognitive_style,
                    error_type=error_type,
                    reference_content=reference_content,
                    source_chapter=source_chapter,
                    weakness_summary=weakness_summary
                )
                if resource:
                    results.append(resource)
        return results

    def _generate_doc(self, knowledge_point: str, cognitive_style: str,
                      reference_content: str, source_chapter: str,
                      weakness_summary: str, **kwargs) -> Optional[dict]:
        prompt = self.prompts["doc"] \
            .replace("{knowledge_point}", knowledge_point) \
            .replace("{cognitive_style}", cognitive_style) \
            .replace("{reference_content}", reference_content[:1500]) \
            .replace("{source_chapter}", source_chapter) \
            .replace("{weakness_summary}", weakness_summary or "暂无")

        content = llm_client.chat(user_message=prompt)
        return {
            "resource_id": str(uuid.uuid4()),
            "resource_type": "doc",
            "knowledge_point": knowledge_point,
            "title": f"【讲解】{knowledge_point}",
            "content": content,
            "cognitive_style_target": cognitive_style,
            "source_references": [source_chapter] if source_chapter else [],
            "generated_at": datetime.now().isoformat(),
            "reading_time": max(3, len(content) // 300)  # 估算阅读分钟数
        }

    def _generate_exercise(self, knowledge_point: str, error_type: str,
                            reference_content: str, **kwargs) -> Optional[dict]:
        # 根据错误类型选择题型
        type_map = {
            "concept_confusion": "choice",
            "flow_omission": "scenario",
            "layer_misplacement": "choice",
            "field_misunderstanding": "fill_blank",
            "": "choice"
        }
        exercise_type = type_map.get(error_type, "choice")

        prompt = self.prompts["exercise"] \
            .replace("{knowledge_point}", knowledge_point) \
            .replace("{error_type}", error_type or "general") \
            .replace("{exercise_type}", exercise_type) \
            .replace("{reference_content}", reference_content[:1000])

        result_str = llm_client.chat(user_message=prompt)
        try:
            clean = result_str.strip().strip("```json").strip("```").strip()
            exercise_data = json.loads(clean)
        except Exception:
            exercise_data = {
                "question": f"关于{knowledge_point}，以下说法正确的是？",
                "options": [{"label": "A", "text": "（题目生成失败，请重试）", "is_distractor": False}],
                "correct_answer": "A",
                "explanation": "解析生成失败",
                "targeted_misconception": error_type
            }

        return {
            "resource_id": str(uuid.uuid4()),
            "resource_type": "exercise",
            "knowledge_point": knowledge_point,
            "title": f"【练习】{knowledge_point}",
            "exercise_type": exercise_type,
            "generated_at": datetime.now().isoformat(),
            **exercise_data
        }

    def _generate_code(self, knowledge_point: str, reference_content: str,
                       **kwargs) -> Optional[dict]:
        prompt = self.prompts["code"] \
            .replace("{knowledge_point}", knowledge_point) \
            .replace("{reference_content}", reference_content[:1000])

        result_str = llm_client.chat(user_message=prompt)
        try:
            clean = result_str.strip().strip("```json").strip("```").strip()
            code_data = json.loads(clean)
        except Exception:
            code_data = {
                "title": f"{knowledge_point} 代码示例",
                "language": "python",
                "code": f"# {knowledge_point} 代码生成失败，请重试\nprint('Hello, TCP!')",
                "explanation": "代码解析失败",
                "expected_output": "",
                "wireshark_filter": "",
                "think_question": ""
            }

        return {
            "resource_id": str(uuid.uuid4()),
            "resource_type": "code",
            "knowledge_point": knowledge_point,
            "title": code_data.get("title", f"【代码】{knowledge_point}"),
            "generated_at": datetime.now().isoformat(),
            **code_data
        }


resource_generator = ResourceGeneratorAgent()
```

**新增 FastAPI 路由** `src/loopse/api/resources.py`：

```python
from fastapi import APIRouter
from pydantic import BaseModel
from typing import List, Optional
from src.loopse.agents.resource_generator import resource_generator
from src.loopse.agents.retriever import retriever_agent

router = APIRouter(prefix="/api/v1/resources")

class GenerateRequest(BaseModel):
    knowledge_point: str
    resource_types: List[str] = ["doc", "exercise", "code"]
    cognitive_style: str = "textual"
    error_type: str = ""
    session_id: Optional[str] = None

@router.post("/generate")
async def generate_resources(req: GenerateRequest):
    # 检索相关知识库内容
    search_results = retriever_agent.search_knowledge(req.knowledge_point, n_results=3)
    reference_content = "\n\n".join([r["content"] for r in search_results])
    source_chapter = search_results[0]["metadata"].get("chapter", "") if search_results else ""

    resources = resource_generator.generate(
        knowledge_point=req.knowledge_point,
        resource_types=req.resource_types,
        cognitive_style=req.cognitive_style,
        error_type=req.error_type,
        reference_content=reference_content,
        source_chapter=source_chapter
    )
    return {"resources": resources, "count": len(resources)}

@router.get("")
async def list_resources(session_id: Optional[str] = None):
    # Phase 4：从DB查询本session生成的资源
    return {"resources": [], "message": "Phase 4: 将从DB查询"}
```

在 `src/loopse/main.py` 中注册：

```python
from src.loopse.api.resources import router as resources_router
app.include_router(resources_router)
```

**写单元测试** `tests/unit/test_resource_generator.py`：

```python
from unittest.mock import patch
from src.loopse.agents.resource_generator import ResourceGeneratorAgent

def test_generate_doc_returns_dict():
    agent = ResourceGeneratorAgent()
    with patch.object(agent.llm_client if hasattr(agent, 'llm_client') else type('m', (), {})(), 'chat', return_value="# TCP三次握手\n\n内容..."):
        # Mock llm_client.chat
        import src.loopse.core.llm_client as lm
        with patch.object(lm, 'llm_client') as mock_llm:
            mock_llm.chat.return_value = "# TCP三次握手\n\n详细讲解内容..."
            result = agent._generate_doc(
                knowledge_point="TCP三次握手",
                cognitive_style="textual",
                reference_content="TCP建立连接需要三次握手...",
                source_chapter="第5章",
                weakness_summary=""
            )
    assert result is not None
    assert result["resource_type"] == "doc"
    assert "resource_id" in result

def test_generate_exercise_fallback_on_json_error():
    """LLM返回非JSON时，应返回兜底题目而不是抛异常"""
    agent = ResourceGeneratorAgent()
    import src.loopse.core.llm_client as lm
    with patch.object(lm, 'llm_client') as mock_llm:
        mock_llm.chat.return_value = "这不是JSON格式"
        result = agent._generate_exercise(
            knowledge_point="TCP三次握手",
            error_type="flow_omission",
            reference_content="内容"
        )
    assert result is not None
    assert result["resource_type"] == "exercise"
```

**交付检查点**：`resource_generator.py` 提交dev，单元测试通过；`/api/v1/resources/generate` 接口可用（Swagger UI可调用）。

---

### 🎨 C — 5月26日

**全天：开发3类资源卡片组件（DocCard / ExerciseCard / CodeCard）**

**`frontend/src/components/resources/DocCard.vue`**：

```vue
<template>
  <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
    <div class="flex items-center justify-between px-4 py-3 border-b border-gray-100 bg-blue-50">
      <div class="flex items-center gap-2">
        <span class="text-blue-500 text-lg">📄</span>
        <span class="font-semibold text-gray-800 text-sm">{{ resource.title }}</span>
      </div>
      <div class="flex items-center gap-2">
        <span class="text-xs text-gray-400">{{ resource.reading_time }}分钟阅读</span>
        <button @click="isExpanded = !isExpanded"
                class="text-xs text-blue-500 hover:text-blue-700 transition-colors">
          {{ isExpanded ? '收起' : '展开' }}
        </button>
      </div>
    </div>
    <div v-show="isExpanded" class="p-4">
      <div class="prose prose-sm max-w-none text-gray-700" v-html="renderedContent"></div>
      <div v-if="resource.source_references?.length" class="mt-3 pt-3 border-t border-gray-100">
        <span class="text-xs text-gray-400">来源：{{ resource.source_references.join('、') }}</span>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import MarkdownIt from 'markdown-it'
import type { DocResource } from '@/types/resource'

const props = defineProps<{ resource: DocResource }>()
const isExpanded = ref(true)
const md = new MarkdownIt({ html: false, linkify: true, typographer: true })
const renderedContent = computed(() => md.render(props.resource.content || ''))
</script>
```

**`frontend/src/components/resources/ExerciseCard.vue`**：

```vue
<template>
  <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
    <div class="flex items-center gap-2 px-4 py-3 border-b border-gray-100 bg-amber-50">
      <span class="text-amber-500 text-lg">✏️</span>
      <span class="font-semibold text-gray-800 text-sm">{{ resource.title }}</span>
      <span class="ml-auto text-xs text-amber-600 bg-amber-100 px-2 py-0.5 rounded-full">
        {{ typeLabel }}
      </span>
    </div>
    <div class="p-4">
      <p class="text-gray-800 font-medium mb-3 leading-relaxed">{{ resource.question }}</p>

      <!-- 单选题 -->
      <div v-if="resource.exercise_type === 'choice' && resource.options" class="space-y-2">
        <button
          v-for="opt in resource.options" :key="opt.label"
          @click="!answered && selectOption(opt.label)"
          :class="[
            'w-full text-left px-3 py-2.5 rounded-lg border text-sm transition-all',
            getOptionClass(opt.label)
          ]"
        >
          <span class="font-bold mr-2">{{ opt.label }}.</span>{{ opt.text }}
        </button>
      </div>

      <!-- 情景/填空题 -->
      <div v-else class="mt-2">
        <textarea v-if="!answered" v-model="textAnswer"
                  class="w-full border border-gray-200 rounded-lg p-3 text-sm resize-none h-24 focus:outline-none focus:ring-2 focus:ring-amber-400"
                  placeholder="请输入你的分析..."></textarea>
        <button v-if="!answered" @click="submitText"
                class="mt-2 bg-amber-500 text-white px-4 py-1.5 rounded-lg text-sm hover:bg-amber-600">
          提交
        </button>
      </div>

      <!-- 解析 -->
      <div v-if="answered" class="mt-3 p-3 bg-green-50 border border-green-200 rounded-lg">
        <p class="text-sm font-medium text-green-700 mb-1">
          {{ isCorrect ? '✅ 回答正确！' : `❌ 正确答案：${resource.correct_answer}` }}
        </p>
        <p class="text-sm text-gray-700">{{ resource.explanation }}</p>
        <p v-if="resource.targeted_misconception" class="text-xs text-gray-400 mt-1">
          本题针对误解：{{ resource.targeted_misconception }}
        </p>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import type { ExerciseResource } from '@/types/resource'

const props = defineProps<{ resource: ExerciseResource }>()
const selectedOption = ref<string>('')
const textAnswer = ref('')
const answered = ref(false)
const isCorrect = ref(false)

const typeLabel: Record<string, string> = {
  choice: '单选题', fill_blank: '填空题', scenario: '情景分析', packet_analysis: '报文分析'
}

function selectOption(label: string) {
  selectedOption.value = label
  answered.value = true
  isCorrect.value = label === props.resource.correct_answer
}

function submitText() {
  answered.value = true
  isCorrect.value = false // 文字题默认展示答案
}

function getOptionClass(label: string) {
  if (!answered.value) {
    return selectedOption.value === label
      ? 'border-amber-400 bg-amber-50'
      : 'border-gray-200 hover:border-amber-300 hover:bg-amber-50'
  }
  if (label === props.resource.correct_answer) return 'border-green-400 bg-green-50 text-green-800'
  if (label === selectedOption.value) return 'border-red-400 bg-red-50 text-red-800'
  return 'border-gray-200 text-gray-400'
}
</script>
```

**`frontend/src/components/resources/CodeCard.vue`**：

```vue
<template>
  <div class="bg-gray-900 rounded-xl shadow-sm overflow-hidden">
    <div class="flex items-center justify-between px-4 py-2.5 border-b border-gray-700">
      <div class="flex items-center gap-2">
        <div class="flex gap-1">
          <div class="w-3 h-3 rounded-full bg-red-500"></div>
          <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
          <div class="w-3 h-3 rounded-full bg-green-500"></div>
        </div>
        <span class="text-gray-400 text-xs font-mono">{{ resource.language }}</span>
        <span class="text-gray-300 text-xs ml-2">{{ resource.title }}</span>
      </div>
      <button @click="copyCode"
              class="text-xs text-gray-400 hover:text-white transition-colors flex items-center gap-1">
        {{ copied ? '✅ 已复制' : '📋 复制' }}
      </button>
    </div>
    <div class="p-4 overflow-x-auto">
      <pre class="text-green-300 text-xs leading-relaxed font-mono">{{ resource.code }}</pre>
    </div>
    <div class="px-4 py-3 bg-gray-800 border-t border-gray-700">
      <p class="text-gray-300 text-xs mb-1">{{ resource.explanation }}</p>
      <div v-if="resource.expected_output" class="mt-2 text-xs">
        <span class="text-gray-500">预期输出：</span>
        <code class="text-yellow-300">{{ resource.expected_output }}</code>
      </div>
      <div v-if="resource.think_question" class="mt-2 p-2 bg-blue-900 rounded text-xs text-blue-200">
        💭 思考：{{ resource.think_question }}
      </div>
      <div v-if="resource.wireshark_filter" class="mt-2 text-xs">
        <span class="text-gray-500">Wireshark过滤器：</span>
        <code class="text-orange-300">{{ resource.wireshark_filter }}</code>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import type { CodeResource } from '@/types/resource'

const props = defineProps<{ resource: CodeResource }>()
const copied = ref(false)

async function copyCode() {
  await navigator.clipboard.writeText(props.resource.code)
  copied.value = true
  setTimeout(() => { copied.value = false }, 2000)
}
</script>
```

**`frontend/src/components/resources/ResourceTabBar.vue`**（整合三类卡片）：

```vue
<template>
  <div v-if="resources.length > 0" class="space-y-4">
    <!-- Tab切换 -->
    <div class="flex gap-2 bg-gray-100 p-1 rounded-lg">
      <button v-for="tab in availableTabs" :key="tab.type"
              @click="activeTab = tab.type"
              :class="['flex-1 py-1.5 text-xs rounded-md transition-all font-medium',
                activeTab === tab.type
                  ? 'bg-white shadow text-blue-600'
                  : 'text-gray-500 hover:text-gray-700']">
        {{ tab.icon }} {{ tab.label }} ({{ tab.count }})
      </button>
    </div>
    <!-- 资源卡片 -->
    <template v-for="resource in currentResources" :key="resource.resource_id">
      <DocCard v-if="resource.resource_type === 'doc'" :resource="resource as DocResource" />
      <ExerciseCard v-else-if="resource.resource_type === 'exercise'" :resource="resource as ExerciseResource" />
      <CodeCard v-else-if="resource.resource_type === 'code'" :resource="resource as CodeResource" />
    </template>
  </div>
  <div v-else class="text-center text-gray-400 text-sm py-8">
    对话后将自动生成个性化学习资源...
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import DocCard from './DocCard.vue'
import ExerciseCard from './ExerciseCard.vue'
import CodeCard from './CodeCard.vue'
import type { BaseResource, DocResource, ExerciseResource, CodeResource } from '@/types/resource'

const props = defineProps<{ resources: BaseResource[] }>()
const activeTab = ref('doc')

const availableTabs = computed(() => [
  { type: 'doc', label: '讲解', icon: '📄', count: props.resources.filter(r => r.resource_type === 'doc').length },
  { type: 'exercise', label: '练习', icon: '✏️', count: props.resources.filter(r => r.resource_type === 'exercise').length },
  { type: 'code', label: '代码', icon: '💻', count: props.resources.filter(r => r.resource_type === 'code').length },
].filter(t => t.count > 0))

const currentResources = computed(() =>
  props.resources.filter(r => r.resource_type === activeTab.value)
)
</script>
```

**交付检查点**：3类资源卡片 + ResourceTabBar 提交dev；在 `ResourcesView.vue` 中引入 `ResourceTabBar`，传入 mock 数据可正常渲染3类卡片截图。

---

## 📅 5月27日（周二）—— B攻Path Planner，C攻路径时间线

### 👨‍💻 A — 5月27日

**全天：完善测试说明书 + 整理三大核心创新点落地文档**

新建 `docs/requirements/核心创新点落地方案.md`：

```markdown
# 三大核心创新点技术落地说明

## 创新点一：对话式动态画像（8维协议能力模型）
### 实现方式
- Profiler Agent：基于对话历史LLM抽取8个维度，增量更新
- 存储：student_profiles表，JSON格式
- 展示：ProfileRadar雷达图实时刷新

### 与竞品的差异化
- Khanmigo/Duolingo Max：仅单维度进度追踪
- 本系统：8个协议专属维度（包含认知风格、误解模式）

### 演示路径（答辩用）
[具体操作步骤，5步内完成演示]

## 创新点二：认知诊断驱动的多表征资源生成
### 实现方式
- Diagnosis Agent三层诊断：表面错误→根因→误解模式
- 根据错误类型选择题型（concept_confusion→选择题，flow_omission→情景题）
- 根据认知风格差异化生成（visual/textual/practical/analogical）

### 演示路径
[步骤]

## 创新点三：可解释学习路径规划
### 实现方式
- 基于JSON知识图谱拓扑排序 + 薄弱点优先
- 每个路径节点附带结构化推荐理由（引用画像证据）
- PathTimeline可视化展示 + 推荐理由弹窗

### 演示路径
[步骤]
```

**交付检查点**：文档提交dev，三大创新点各≥300字，演示路径具体可操作。

---

### 🧠 B — 5月27日

**全天：开发 `src/loopse/agents/path_planner.py`**

```python
# src/loopse/agents/path_planner.py
"""
Path Planner Agent — 可解释学习路径规划
算法：拓扑排序 + 薄弱点优先 + LLM辅助生成推荐理由
"""
import json, uuid
from datetime import datetime
from pathlib import Path
from typing import List, Optional
from src.loopse.kb.knowledge_graph import knowledge_graph, KnowledgeGraph
from src.loopse.core.llm_client import llm_client

PLAN_PROMPT = Path("config/prompts/path_planner/plan_path.txt").read_text(encoding="utf-8") \
    if Path("config/prompts/path_planner/plan_path.txt").exists() else ""


class PathPlannerAgent:

    def __init__(self, kg: KnowledgeGraph = None):
        self.kg = kg or knowledge_graph

    def plan(
        self,
        user_id: str,
        profile: dict,
        target_topic: str = "TCP/IP协议",
        diagnosis_result: Optional[dict] = None,
        max_nodes: int = 6
    ) -> dict:
        """
        核心规划逻辑：
        1. 确定目标知识点（从topic关键词匹配图谱节点）
        2. 找出所有前置节点中的薄弱点
        3. 拓扑排序 + 薄弱点优先
        4. 为每个节点LLM生成推荐理由
        """
        # Step 1：确定目标节点
        target_nodes = self._find_target_nodes(target_topic, diagnosis_result)

        # Step 2：收集前置薄弱点
        weak_nodes = []
        seen = set()
        for tnid in target_nodes:
            for wn in self.kg.find_weak_prerequisites(tnid, profile, threshold=0.6):
                if wn["node_id"] not in seen:
                    weak_nodes.append(wn)
                    seen.add(wn["node_id"])

        # 合并目标节点自身（如果画像也未达标）
        for tnid in target_nodes:
            node = self.kg.get_node(tnid)
            if node and tnid not in seen:
                mastery = self.kg._estimate_mastery(tnid, profile)
                if mastery < 0.7:
                    weak_nodes.append({
                        "node_id": tnid,
                        "node_name": node["name"],
                        "chapter": node.get("chapter", ""),
                        "mastery": mastery,
                        "estimated_time": node.get("estimated_time", 45)
                    })
                    seen.add(tnid)

        if not weak_nodes:
            # 没有薄弱点，推荐进阶内容
            return self._plan_advanced(user_id, profile, target_nodes)

        # Step 3：拓扑排序
        all_node_ids = [n["node_id"] for n in weak_nodes]
        sorted_ids = self.kg.topological_sort(all_node_ids)
        # 未能排序的节点追加到末尾
        remaining = [nid for nid in all_node_ids if nid not in sorted_ids]
        sorted_ids = sorted_ids + remaining

        # Step 4：限制节点数量，薄弱点优先
        weak_set = {n["node_id"] for n in weak_nodes}
        final_ids = sorted_ids[:max_nodes]

        # Step 5：生成路径节点（带推荐理由）
        path_nodes = []
        for nid in final_ids:
            node = self.kg.get_node(nid)
            if not node:
                continue
            wn_data = next((w for w in weak_nodes if w["node_id"] == nid), {})
            reason = self._generate_reason(nid, node, wn_data, profile, diagnosis_result)
            prerequisites_met = all(
                self.kg._estimate_mastery(pre, profile) >= 0.5
                for pre in self.kg.get_prerequisites(nid)
            )
            path_nodes.append({
                "node_id": nid,
                "node_name": node["name"],
                "type": "learning",
                "chapter": node.get("chapter", ""),
                "estimated_time": node.get("estimated_time", 30),
                "recommendation_reason": reason,
                "reason_sources": [f"画像维度证据", f"知识图谱前置依赖"],
                "suggested_resources": ["doc", "exercise"],
                "prerequisites": self.kg.get_prerequisites(nid),
                "prerequisites_met": prerequisites_met,
                "status": "pending" if prerequisites_met else "locked",
                "current_mastery": wn_data.get("mastery", 0.0)
            })

        return {
            "path_id": str(uuid.uuid4()),
            "user_id": user_id,
            "title": f"针对「{target_topic}」的个性化学习路径",
            "description": f"根据你的画像，检测到{len(weak_nodes)}个薄弱前置知识点，以下路径将帮助你系统补强。",
            "total_estimated_time": sum(n["estimated_time"] for n in path_nodes),
            "nodes": path_nodes,
            "generated_at": datetime.now().isoformat()
        }

    def _find_target_nodes(self, topic: str, diagnosis_result: Optional[dict]) -> List[str]:
        """从话题关键词或诊断结果中确定目标知识点"""
        target_ids = []

        # 优先从诊断结果提取（如果有root_causes）
        if diagnosis_result and not diagnosis_result.get("is_correct", True):
            for rc in diagnosis_result.get("root_causes", []):
                if rc.get("knowledge_node_id"):
                    target_ids.append(rc["knowledge_node_id"])

        # 从topic关键词搜索
        if not target_ids:
            matches = self.kg.search_by_keyword(topic)
            if not matches:
                # 尝试逐词搜索
                for word in topic.split():
                    matches.extend(self.kg.search_by_keyword(word))
            target_ids = [m["id"] for m in matches[:3]]

        # 兜底：使用TCP三次握手
        if not target_ids:
            target_ids = ["kn_008"]

        return list(set(target_ids))

    def _generate_reason(self, node_id: str, node: dict, wn_data: dict,
                         profile: dict, diagnosis_result: Optional[dict]) -> str:
        """用LLM或规则生成推荐理由（优先规则，避免过多API调用）"""
        mastery = wn_data.get("mastery", 0.0)
        mastery_desc = "掌握度极低" if mastery < 0.3 else ("掌握度偏低" if mastery < 0.6 else "需要巩固")
        node_name = node["name"]

        # 简单规则生成（不消耗API）
        if diagnosis_result and not diagnosis_result.get("is_correct", True):
            error_desc = diagnosis_result.get("surface_error", {}).get("error_description", "")
            if error_desc:
                return f"诊断发现你对「{node_name}」的理解存在偏差（{error_desc[:30]}），建议优先补强此前置知识。"

        return (
            f"根据你的学习画像，「{node_name}」当前{mastery_desc}（评分 {mastery:.1f}/1.0）。"
            f"这是后续学习「{node['chapter']}」核心内容的必要前置知识，建议优先掌握。"
        )

    def _plan_advanced(self, user_id: str, profile: dict, target_nodes: List[str]) -> dict:
        """无薄弱点时，推荐进阶路径"""
        advanced_map = {
            "kn_008": ["kn_009", "kn_010"],  # 三次握手 → 四次挥手、可靠传输
            "kn_006": ["kn_011", "kn_012"],  # TCP基础 → 流量控制、拥塞控制
            "kn_013": ["kn_014", "kn_015"],  # HTTP → HTTPS、DNS
        }
        next_nodes = []
        for tnid in target_nodes:
            next_nodes.extend(advanced_map.get(tnid, []))
        next_nodes = next_nodes[:4] if next_nodes else ["kn_012", "kn_014"]

        path_nodes = []
        for nid in next_nodes:
            node = self.kg.get_node(nid)
            if node:
                path_nodes.append({
                    "node_id": nid,
                    "node_name": node["name"],
                    "type": "learning",
                    "chapter": node.get("chapter", ""),
                    "estimated_time": node.get("estimated_time", 45),
                    "recommendation_reason": f"你已掌握前置知识，「{node['name']}」是下一个自然进阶方向。",
                    "reason_sources": ["知识图谱进阶路径"],
                    "suggested_resources": ["doc", "exercise"],
                    "prerequisites": self.kg.get_prerequisites(nid),
                    "prerequisites_met": True,
                    "status": "pending",
                    "current_mastery": 0.0
                })

        return {
            "path_id": str(uuid.uuid4()),
            "user_id": user_id,
            "title": "进阶学习路径",
            "description": "当前知识点掌握良好，以下是推荐的进阶方向。",
            "total_estimated_time": sum(n["estimated_time"] for n in path_nodes),
            "nodes": path_nodes,
            "generated_at": datetime.now().isoformat()
        }


path_planner = PathPlannerAgent()
```

**新增路径API** `src/loopse/api/path.py`：

```python
from fastapi import APIRouter
from pydantic import BaseModel
from typing import Optional
from src.loopse.agents.path_planner import path_planner
from src.loopse.db.repositories import ProfileRepository

router = APIRouter(prefix="/api/v1/path")

class PlanRequest(BaseModel):
    user_id: str
    target_topic: str = "TCP/IP协议"
    session_id: Optional[str] = None

@router.post("/plan")
async def plan_path(req: PlanRequest):
    profile = ProfileRepository.get(req.user_id) or {}
    result = path_planner.plan(
        user_id=req.user_id,
        profile=profile,
        target_topic=req.target_topic
    )
    return result

@router.get("/{user_id}")
async def get_path(user_id: str):
    # Phase 4：从DB读取已规划路径
    profile = ProfileRepository.get(user_id) or {}
    result = path_planner.plan(user_id=user_id, profile=profile)
    return result

@router.post("/{user_id}/progress")
async def update_progress(user_id: str, node_id: str, status: str):
    # Phase 4：更新节点状态到DB
    return {"success": True, "node_id": node_id, "status": status}
```

**交付检查点**：`path_planner.py` 提交dev，`/api/v1/path/plan` 可调用，返回包含至少3个节点的路径JSON，每节点有 `recommendation_reason`。

---

### 🎨 C — 5月27日

**全天：开发学习路径时间线组件**

**`frontend/src/components/path/PathTimeline.vue`**：

```vue
<template>
  <div v-if="path" class="space-y-1">
    <div class="mb-4">
      <h4 class="font-bold text-gray-800">{{ path.title }}</h4>
      <p class="text-xs text-gray-500 mt-0.5">{{ path.description }}</p>
      <div class="flex items-center gap-4 mt-2">
        <span class="text-xs text-gray-400">
          ⏱ 预计总时长：{{ path.total_estimated_time }} 分钟
        </span>
        <span class="text-xs text-gray-400">
          📍 {{ completedCount }}/{{ path.nodes.length }} 已完成
        </span>
      </div>
    </div>

    <div class="relative">
      <!-- 左侧连线 -->
      <div class="absolute left-4 top-8 bottom-8 w-px bg-gray-200"></div>

      <div v-for="(node, idx) in path.nodes" :key="node.node_id"
           class="relative flex items-start gap-3 pb-4">
        <!-- 节点圆点 -->
        <div :class="[
          'relative z-10 w-8 h-8 rounded-full flex items-center justify-center flex-shrink-0 text-xs font-bold border-2',
          nodeStyle(node.status)
        ]">
          {{ node.status === 'completed' ? '✓' : idx + 1 }}
        </div>

        <!-- 节点内容 -->
        <div :class="[
          'flex-1 rounded-lg p-3 border transition-all cursor-pointer',
          node.status === 'locked' ? 'opacity-50 cursor-not-allowed bg-gray-50 border-gray-200' :
          'bg-white border-gray-200 hover:border-blue-300 hover:shadow-sm'
        ]" @click="node.status !== 'locked' && openReason(node)">
          <div class="flex items-center justify-between">
            <span class="font-medium text-gray-800 text-sm">{{ node.node_name }}</span>
            <div class="flex items-center gap-1.5">
              <span class="text-xs text-gray-400">{{ node.chapter }}</span>
              <span class="text-xs text-gray-400">{{ node.estimated_time }}min</span>
              <span v-if="node.status === 'locked'" class="text-xs">🔒</span>
            </div>
          </div>
          <!-- 推荐理由预览 -->
          <p class="text-xs text-gray-500 mt-1 line-clamp-2">
            💡 {{ node.recommendation_reason }}
          </p>
          <!-- 掌握度进度条 -->
          <div class="mt-2 flex items-center gap-2">
            <div class="flex-1 h-1.5 bg-gray-100 rounded-full overflow-hidden">
              <div class="h-full rounded-full transition-all duration-500"
                   :class="masteryColor(node.current_mastery)"
                   :style="{ width: `${(node.current_mastery || 0) * 100}%` }"></div>
            </div>
            <span class="text-xs text-gray-400">{{ Math.round((node.current_mastery || 0) * 100) }}%</span>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div v-else class="text-center text-gray-400 text-sm py-8">
    开始学习后将自动规划个性化路径...
  </div>

  <!-- 推荐理由弹窗 -->
  <PathReasonModal v-if="selectedNode"
                   :node="selectedNode"
                   @close="selectedNode = null" />
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import PathReasonModal from './PathReasonModal.vue'
import type { LearningPath, PathNode, PathNodeStatus } from '@/types/path'

const props = defineProps<{ path: LearningPath | null }>()
const selectedNode = ref<PathNode | null>(null)

const completedCount = computed(() =>
  props.path?.nodes.filter(n => n.status === 'completed').length ?? 0
)

function openReason(node: PathNode) {
  selectedNode.value = node
}

function nodeStyle(status: PathNodeStatus): string {
  const styles: Record<PathNodeStatus, string> = {
    completed: 'border-green-400 bg-green-400 text-white',
    in_progress: 'border-blue-400 bg-blue-50 text-blue-600',
    pending: 'border-gray-300 bg-white text-gray-500',
    locked: 'border-gray-200 bg-gray-100 text-gray-300',
  }
  return styles[status] || styles.pending
}

function masteryColor(mastery: number): string {
  if (mastery >= 0.7) return 'bg-green-400'
  if (mastery >= 0.4) return 'bg-yellow-400'
  return 'bg-red-400'
}
</script>
```

**`frontend/src/components/path/PathReasonModal.vue`**：

```vue
<template>
  <div class="fixed inset-0 bg-black/40 flex items-center justify-center z-50 p-4"
       @click.self="$emit('close')">
    <div class="bg-white rounded-2xl shadow-xl max-w-md w-full p-6">
      <div class="flex items-center justify-between mb-4">
        <h3 class="font-bold text-gray-800">{{ node.node_name }}</h3>
        <button @click="$emit('close')" class="text-gray-400 hover:text-gray-600">✕</button>
      </div>
      <div class="space-y-3">
        <div class="p-3 bg-blue-50 rounded-lg">
          <p class="text-xs text-blue-600 font-medium mb-1">📍 推荐理由</p>
          <p class="text-sm text-gray-700">{{ node.recommendation_reason }}</p>
        </div>
        <div class="grid grid-cols-2 gap-2 text-xs text-gray-500">
          <div class="bg-gray-50 rounded p-2">
            <span class="block text-gray-400">所属章节</span>
            <span class="font-medium text-gray-700">{{ node.chapter }}</span>
          </div>
          <div class="bg-gray-50 rounded p-2">
            <span class="block text-gray-400">预计时长</span>
            <span class="font-medium text-gray-700">{{ node.estimated_time }} 分钟</span>
          </div>
          <div class="bg-gray-50 rounded p-2">
            <span class="block text-gray-400">当前掌握度</span>
            <span class="font-medium" :class="node.current_mastery >= 0.6 ? 'text-green-600' : 'text-red-500'">
              {{ Math.round((node.current_mastery || 0) * 100) }}%
            </span>
          </div>
          <div class="bg-gray-50 rounded p-2">
            <span class="block text-gray-400">前置是否满足</span>
            <span class="font-medium" :class="node.prerequisites_met ? 'text-green-600' : 'text-amber-500'">
              {{ node.prerequisites_met ? '✅ 已满足' : '⚠️ 待补强' }}
            </span>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import type { PathNode } from '@/types/path'
defineProps<{ node: PathNode }>()
defineEmits(['close'])
</script>
```

**在 `PathView.vue` 接通 API**：

```vue
<script setup lang="ts">
import { onMounted } from 'vue'
import PathTimeline from '@/components/path/PathTimeline.vue'
import { usePathStore } from '@/stores/pathStore'
import { useUserStore } from '@/stores/userStore'

const pathStore = usePathStore()
const userStore = useUserStore()

onMounted(() => {
  pathStore.fetchPath(userStore.userId)
})
</script>
<template>
  <div class="max-w-2xl mx-auto p-6">
    <h2 class="text-xl font-bold text-gray-800 mb-6">📚 我的学习路径</h2>
    <PathTimeline :path="pathStore.path" />
  </div>
</template>
```

**交付检查点**：`/path` 页面能加载真实路径数据（从 `/api/v1/path/{userId}` 获取），路径时间线正常渲染，点击节点弹出推荐理由弹窗。

---

## 📅 5月28日（周三）—— Orchestrator 接入新Agent + 全链路整合

### 👨‍💻 A — 5月28日

**全天：SRS补充 + 确认所有API路由完整性**

更新 `docs/requirements/SRS_v1.md`，新增第五章：

```markdown
## 第五章 Phase 3 新增功能规格

### 5.1 Resource Generator Agent
- 支持3类资源：讲解文档(doc)、练习题(exercise)、代码示例(code)
- 资源生成基于知识库检索结果，禁止凭空捏造
- 根据认知风格调整生成策略

### 5.2 Path Planner Agent  
- 基于JSON知识图谱（20节点，谢希仁第8版5章）
- 算法：前置依赖分析 + 拓扑排序 + 薄弱点优先
- 每节点必须输出结构化推荐理由（引用画像证据）

### 5.3 完整链路数据流
用户输入 → Orchestrator路由 → Retriever检索 → [Diagnosis诊断] →
Generator生成资源 → Planner规划路径 → SSE事件流 → 前端多面板展示
```

同时做路由完整性检查：列出所有已实现的API路由，与后端总领文档M14对比，标注哪些本周完成、哪些Phase 4完成。

---

### 🧠 B — 5月28日

**全天：Orchestrator 接入 Resource Generator + Path Planner**

更新 `src/loopse/agents/orchestrator.py`，将资源生成和路径规划纳入Agent链：

```python
# 在 OrchestratorAgent 中扩展 _build_agent_chain
RESOURCE_KEYWORDS = ["给我", "生成", "总结一下", "思维导图", "练习题", "代码示例", "怎么学"]
PLANNING_KEYWORDS = ["先学什么", "学习计划", "建议我", "我该", "路径", "从哪里开始"]

def _build_agent_chain(self, phase: SessionPhase, user_message: str) -> list:
    chains = {
        SessionPhase.PROFILING: ["profiler", "retriever"],
        SessionPhase.SIMULATING: ["retriever", "simulator"],
        SessionPhase.PLANNING: ["profiler", "planner"],
        SessionPhase.LEARNING: ["retriever"],
        SessionPhase.DIAGNOSING: ["retriever", "diagnosis", "generator"],  # 诊断后自动生成资源
    }
    chain = chains.get(phase, ["retriever"])

    # 动态追加
    if any(kw in user_message for kw in RESOURCE_KEYWORDS) and "generator" not in chain:
        chain.append("generator")
    if any(kw in user_message for kw in PLANNING_KEYWORDS) and "planner" not in chain:
        chain = ["profiler", "planner"]  # 路径规划时替换整个链

    return chain
```

在 `process_message` 中新增 Generator 和 Planner 的执行逻辑：

```python
elif agent_name == "generator":
    from src.loopse.agents.resource_generator import resource_generator
    knowledge_point = self._extract_knowledge_point(user_message, context)
    error_type = context.get("diagnosis_result", {}).get("surface_error", {}).get("error_type", "")
    ref_text = "\n\n".join([r["content"] for r in context.get("retrieval_context", [])[:3]])
    
    resources = resource_generator.generate(
        knowledge_point=knowledge_point,
        resource_types=["doc", "exercise", "code"],
        cognitive_style=context.get("profile", {}).get("cognitive_style", "textual"),
        error_type=error_type,
        reference_content=ref_text,
    )
    context["generated_resources"] = resources
    
    # 每生成一个资源，发送一个SSE事件
    for r in resources:
        yield self._sse_event("resource", "Generator", r)

elif agent_name == "planner":
    from src.loopse.agents.path_planner import path_planner
    path = path_planner.plan(
        user_id=context.get("user_id", "demo"),
        profile=context.get("profile", {}),
        target_topic=user_message,
        diagnosis_result=context.get("diagnosis_result")
    )
    context["learning_path"] = path
    yield self._sse_event("path_update", "Planner", path)

def _extract_knowledge_point(self, user_message: str, context: dict) -> str:
    """从消息和检索结果提取主要知识点"""
    if context.get("diagnosis_result"):
        rc = context["diagnosis_result"].get("root_causes", [])
        if rc:
            return rc[0].get("knowledge_node_name", user_message[:20])
    # 从检索结果的metadata提取
    retrieval = context.get("retrieval_context", [])
    if retrieval:
        kp = retrieval[0].get("metadata", {}).get("chapter", "")
        if kp:
            return kp
    return user_message[:30]
```

**交付检查点**：输入"给我讲讲TCP三次握手"，Orchestrator 调用链包含 `retriever→generator`，SSE流中出现 `resource` 类型事件，内容包含doc/exercise/code三类资源。

---

### 🎨 C — 5月28日

**全天：前端接收 resource 和 path_update SSE事件 + 整合展示**

在 `useSSE.ts` 或 `chatStore.ts` 中扩展对新事件类型的处理：

```typescript
// 扩展事件处理，新增 resource 和 path_update
case 'resource': {
  const resourceData = typeof event.data === 'string'
    ? JSON.parse(event.data) : event.data
  resourceStore.addResource(resourceData)
  break
}
case 'path_update': {
  const pathData = typeof event.data === 'string'
    ? JSON.parse(event.data) : event.data
  pathStore.updateFromSSE(pathData)
  break
}
```

在 `ChatView.vue` 中，对话界面右侧增加资源面板：

```vue
<template>
  <div class="flex h-screen bg-gray-50">
    <!-- 主聊天区 -->
    <div class="flex flex-col flex-1 max-w-2xl border-r border-gray-200">
      <!-- 消息列表... -->
      <!-- Agent状态条 -->
      <AgentStatusBar :active-agents="chatStore.activeAgents" />
      <!-- 输入框... -->
    </div>

    <!-- 右侧面板（画像 + 资源 + 路径）-->
    <div class="w-96 flex flex-col overflow-hidden bg-white">
      <!-- Tab切换 -->
      <div class="flex border-b border-gray-100">
        <button v-for="tab in sideTabs" :key="tab.id"
                @click="sideTab = tab.id"
                :class="['flex-1 py-2 text-xs font-medium border-b-2 transition-colors',
                  sideTab === tab.id ? 'border-blue-500 text-blue-600' : 'border-transparent text-gray-400']">
          {{ tab.icon }} {{ tab.label }}
        </button>
      </div>
      <!-- 面板内容 -->
      <div class="flex-1 overflow-y-auto p-3">
        <ProfileRadar v-if="sideTab === 'profile'" />
        <ResourceTabBar v-if="sideTab === 'resources'" :resources="resourceStore.resources" />
        <PathTimeline v-if="sideTab === 'path'" :path="pathStore.path" />
        <AgentLogPanel v-if="sideTab === 'logs'" :session-id="chatStore.sessionId" />
      </div>
    </div>
  </div>
</template>
```

**交付检查点**：ChatView 右侧面板能在对话中自动切换到 resources tab 并显示生成的资源卡片；path tab 能展示路径时间线。

---

## 📅 5月29日（周四）—— 全链路联调日（第二次）

**目标：跑通 "画像→检索→诊断→资源生成→路径规划→前端展示" 完整链路**

### 联调步骤（严格按顺序，不要跳过）

**Step 1（B）：确认后端完整启动**

```bash
source .venv/bin/activate
python scripts/init_db.py      # 确保表存在
uvicorn src.loopse.main:app --reload --port 8000
# 访问 http://localhost:8000/docs 验证所有路由存在
```

确认存在的路由（至少）：
- `GET /health`
- `POST /api/v1/chat/stream`
- `GET /api/v1/profile/{user_id}`
- `POST /api/v1/resources/generate`
- `POST /api/v1/path/plan`
- `GET /api/v1/logs/session/{session_id}`

**Step 2（C）：前端启动并确认代理**

```bash
cd frontend && npm run dev
# 确认 /api/v1/health 通过代理返回200（不是CORS错误）
```

**Step 3（三人）：4个核心测试场景依次运行**

| 场景 | 输入 | 期望 Agent链 | 期望前端变化 |
|------|------|------------|------------|
| 📖 知识问答 | "什么是TCP三次握手" | Orchestrator→Retriever→LLM | 流式文字显示，Agent状态条有记录 |
| ❌ 错误诊断 | "HTTP不需要TCP吧，直接基于IP" | Orchestrator→Retriever→Diagnosis→Generator→LLM | 诊断标签出现，resources tab有doc+exercise+code |
| 🗺 路径请求 | "我该先学什么才能学好TCP" | Orchestrator→Profiler→Planner | path tab 显示路径，点击节点弹窗有推荐理由 |
| 💻 代码请求 | "给我一个Python实现TCP socket的代码示例" | Orchestrator→Retriever→Generator | resources tab有code卡片，代码可复制 |

**Step 4（A）：记录联调问题**

新建 `docs/results/phase3_issues.md`，格式同phase1_issues.md。

**Step 5（B + C）：当场修复P0级Bug**

P0级定义：链路断裂（无法继续下一步）或前端白屏。P1级（显示异常但不影响流程）留到5/30修复。

---

## 📅 5月30日（周五）—— Bug修复 + AgentStatusBar + 离线Mock准备

### 👨‍💻 A — 5月30日

**全天：创新点落地方案文档完善 + 离线Mock数据规划**

更新 `docs/requirements/核心创新点落地方案.md`，将三大创新点的演示路径细化为可操作的7步流程：

```markdown
## 答辩演示脚本（7分钟，三大创新点顺序展示）

### 段落1：对话式动态画像（2分钟）
操作：输入3个不同深度的TCP问题，观察雷达图实时变化
展示：profile tab的雷达图，6个维度评分逐步更新

### 段落2：认知诊断+多表征资源（3分钟）
操作：输入一个错误说法
展示：诊断标签（错误类型+误解模式）+ resources tab三类资源卡片

### 段落3：可解释路径规划（2分钟）
操作：输入"我该先学什么"
展示：path tab时间线 + 点击节点查看推荐理由弹窗
```

---

### 🧠 B — 5月30日

**上午：修复5/29联调发现的P1级Bug**

**下午：整合 Diagnosis 根因分析与知识图谱**

目前 `DiagnosisAgent._analyze_root_cause` 的 `knowledge_graph_context` 参数是空字符串，今天接入真实图谱数据：

```python
# 在 orchestrator.py 中调用 diagnosis 时，传入知识图谱上下文
from src.loopse.kb.knowledge_graph import knowledge_graph

# 获取与用户问题相关的知识点依赖上下文
def _get_kg_context(self, user_message: str) -> str:
    matches = knowledge_graph.search_by_keyword(user_message[:10])
    if not matches:
        return ""
    context_parts = []
    for node in matches[:3]:
        pres = knowledge_graph.get_prerequisites(node["id"])
        pre_names = [knowledge_graph.get_node(p)["name"] for p in pres if knowledge_graph.get_node(p)]
        context_parts.append(
            f"- 知识点「{node['name']}」的前置依赖：{', '.join(pre_names) or '无'}"
        )
    return "\n".join(context_parts)
```

---

### 🎨 C — 5月30日

**上午：修复5/29联调发现的前端P1级Bug**

**下午：准备离线Mock数据（Phase 5正式制作，今天搭框架）**

在 `frontend/public/mock/` 下创建Mock数据文件框架，填入5/29联调中实际获得的真实API响应数据（直接复制终端输出即可，不需要手写）：

- `public/mock/chat_mock.json`：一次完整对话的SSE事件流（token+diagnosis+resource+path_update）
- `public/mock/profile_mock.json`：画像API的响应
- `public/mock/path_mock.json`：路径API的响应
- `public/mock/resources_mock.json`：资源生成API的响应（包含3类资源各1个）

**完善 `AgentStatusBar.vue` 组件**：

```vue
<template>
  <div v-if="activeAgents.length > 0"
       class="flex items-center gap-2 px-4 py-2 bg-blue-50 border-t border-blue-100">
    <span class="text-xs text-blue-400">Agent协作中：</span>
    <div class="flex gap-1.5">
      <div v-for="agent in activeAgents" :key="agent"
           class="flex items-center gap-1 bg-white border border-blue-200 rounded-full px-2 py-0.5">
        <div class="w-1.5 h-1.5 rounded-full bg-blue-400 animate-pulse"></div>
        <span class="text-xs text-blue-600 font-medium">{{ agent }}</span>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
defineProps<{ activeAgents: string[] }>()
</script>
```

**交付检查点**：Mock数据文件框架创建完成；AgentStatusBar 在对话中显示正在运行的Agent名称，有呼吸动画。

---

## 📅 5月31日（周六）—— Phase 3 验收日

**上午：各人自查 + 最终补齐**

**下午：A 主持验收例会，逐项检查清单**

---

## ✅ Phase 3 验收清单（5月31日）

### 必须全绿（Hard）

**⬜ 知识图谱**
- [ ] `data/knowledge_graph.json` 存在，≥20个节点，≥18条边，覆盖5章
- [ ] `src/loopse/kb/knowledge_graph.py` 可 import，`knowledge_graph.get_all_prerequisites("kn_008")` 返回非空列表
- [ ] `topological_sort` 对 `["kn_006", "kn_007", "kn_008"]` 返回正确顺序（kn_006在最前）

**⬜ Resource Generator Agent**
- [ ] `POST /api/v1/resources/generate` 调用成功，返回包含 doc/exercise/code 3类资源
- [ ] doc 资源 `content` 字段为非空 Markdown 文本，含知识点来源引用
- [ ] exercise 资源包含 `question / options / correct_answer / explanation` 字段
- [ ] code 资源包含 `code / explanation / expected_output` 字段
- [ ] `tests/unit/test_resource_generator.py` 全部通过（mock模式）

**⬜ Path Planner Agent**
- [ ] `POST /api/v1/path/plan` 调用成功，返回路径含 ≥3 个节点
- [ ] 每个节点有非空 `recommendation_reason`（≥20字）
- [ ] 对画像为空的用户，路径仍能返回（不报错）
- [ ] 对画像满分的用户，路径返回进阶方向（不返回已完成的节点）

**⬜ 全链路端到端**
- [ ] 输入"什么是TCP三次握手"：SSE事件流含 agent_start/tool_call/token/agent_end/done，前端流式显示
- [ ] 输入"HTTP不需要TCP吧"：SSE含 diagnosis 事件，前端诊断标签显示；SSE含 resource 事件，resources tab 有3类卡片
- [ ] 输入"我该先学什么"：SSE含 path_update 事件，path tab 显示路径时间线，点击节点弹出推荐理由
- [ ] Agent 日志面板：`GET /api/v1/logs/session/{id}` 返回 ≥4 条不同Agent的日志

**⬜ 前端展示**
- [ ] DocCard：展开/收起功能正常，Markdown渲染正常（含换行/列表/加粗）
- [ ] ExerciseCard：点击选项后显示解析，正确答案高亮绿色，错误选项高亮红色
- [ ] CodeCard：代码区正常显示，"复制"按钮点击后变为"已复制"
- [ ] PathTimeline：路径节点正常渲染，状态色区分（pending/locked/completed）
- [ ] PathReasonModal：点击节点弹窗显示推荐理由、章节、预计时长、前置满足状态
- [ ] AgentStatusBar：对话中显示正在运行的Agent名称（有动画）

**⬜ 工程质量**
- [ ] 所有新增Prompt均通过文件加载（无硬编码字符串在Python代码中）
- [ ] 所有新增API路由已注册到 `main.py`，Swagger UI可见
- [ ] `npm run build` 无 TypeScript 类型错误

### 争取完成（Soft）

- [ ] Profiler Agent 在对话3轮后，雷达图有可见变化（画像有更新）
- [ ] 资源生成根据认知风格差异化（visual用户的doc资源有更多图解引导）
- [ ] `公/mock/` 下 4 个mock文件均有真实数据填充（含3类资源 + 路径 + 诊断）
- [ ] `docs/test/测试说明书_v1.md` 第二章验收用例≥5个，每个有明确通过标准
- [ ] `docs/requirements/核心创新点落地方案.md` 三大创新点各≥300字，有演示路径

---

## 📋 Phase 3 总交付物一览

| 文件/功能 | 负责人 | 截止 | 状态 |
|-----------|--------|------|------|
| `data/knowledge_graph.json`（20节点，18边） | A | 5/25 | |
| `docs/test/测试说明书_v1.md` 框架 | A | 5/25 | |
| `docs/test/phase3_acceptance_cases.md` | A | 5/26 | |
| `docs/requirements/核心创新点落地方案.md` | A | 5/27 | |
| `docs/requirements/SRS_v1.md` Phase 3章节 | A | 5/28 | |
| `src/loopse/kb/knowledge_graph.py` | B | 5/25 | |
| `config/prompts/resource_generator/generate_*.txt`（3个） | B | 5/25 | |
| `src/loopse/agents/resource_generator.py` | B | 5/26 | |
| `src/loopse/api/resources.py` | B | 5/26 | |
| `tests/unit/test_resource_generator.py` | B | 5/26 | |
| `src/loopse/agents/path_planner.py` | B | 5/27 | |
| `src/loopse/api/path.py` | B | 5/27 | |
| Orchestrator接入Generator+Planner | B | 5/28 | |
| `frontend/src/types/{resource,path,diagnosis}.ts` | C | 5/25 | |
| `frontend/src/api/{resources,path}.ts` | C | 5/25 | |
| `frontend/src/stores/{resourceStore,pathStore}.ts` | C | 5/25 | |
| `DocCard.vue / ExerciseCard.vue / CodeCard.vue` | C | 5/26 | |
| `ResourceTabBar.vue` | C | 5/26 | |
| `PathTimeline.vue / PathReasonModal.vue` | C | 5/27 | |
| `PathView.vue` 接通API | C | 5/27 | |
| `ChatView.vue` 右侧面板整合 | C | 5/28 | |
| 前端 SSE resource/path_update 事件解析 | C | 5/28 | |
| `AgentStatusBar.vue` 完善 | C | 5/30 | |
| `public/mock/` 4个文件框架 | C | 5/30 | |
| 全链路联调 + phase3_issues.md | 三人 | 5/29 | |

**5月31日下午验收清单 Hard 部分全绿 = Phase 3通过，Phase 4（补齐官方硬指标）正式起跑。**

> （注：文档部分内容可能由 AI 生成）
