# Phase 4 详细任务安排（6 月 1 日 — 6 月 7 日）

先读取这 4 个阶段的任务文件，确认你们已完成的内容，然后生成 Phase 4 的详细任务清单。

已确认 Phase 0–3 全部完成。现在为你生成 **Phase 4（6 月 1 日 — 6 月 7 日）** 的逐日详细任务清单。

---

# Phase 4 详细任务安排（6 月 1 日 — 6 月 7 日）

> **核心目标**：补齐官方全部硬指标，诊断引擎三层跑通、5 类资源全量生成、协议仿真扩展至 5\+ 场景、系统可信机制上线
> **验收标准**（硬性，6 月 7 日晚逐项打勾）：
> 
> 1. 5 类资源（文档 / 思维导图 / 练习题 / 代码 / 视频脚本）全部可稳定生成
> 
> 2. Diagnosis Agent 三层诊断模型完整运行：表面错误 → 根因溯源 → 误解模式匹配
> 
> 3. 协议仿真场景 ≥ 5 个（三次握手、四次挥手、滑动窗口、HTTP 分层、DNS 解析）
> 
> 4. 系统可信机制可正常工作：越界回答拦截 \+ 引用溯源 \+ 不确定性声明
> 
> 5. 官方硬指标清单 100% 打勾（8 维画像 / 8 Agent 协同 / 5 类资源 / 路径规划 / 防幻觉 / 自建知识库）
> 
> 

---

## ⚠️ 6 月 1 日（周日）上午：Phase 3 清账 \+ Phase 4 对齐会

**三人必须先开 30 分钟对齐会**，逐项确认 Phase 3 收尾状态，有欠债当场清：

|确认项|负责人|验证方式|
|---|---|---|
|Resource Generator 3 类资源（文档 / 练习 / 代码）是否可生成|B|调接口返回完整 JSON|
|Path Planner 路径是否附带推荐理由|B|返回节点含 `recommendation_reason` 字段|
|Diagnosis 根因溯源层（第二层）是否跑通|B|输入错误答案，返回 `root_causes` 数组|
|3 类资源卡片前端渲染是否正常|C|前端页面切换无报错|
|路径时间线组件是否可展示理由|C|点击节点弹出理由弹窗|
|Challenger Agent v1 是否可发起 3 轮对话|B|完整跑一轮对抗流程|
|协议仿真与对话联动是否打通|C|问流程问题自动触发仿真播放|

确认后，A 记录遗留问题到 `docs/results/phase3_issues.md`，当场分配补救任务，**不带着欠债进入 Phase 4 正式开发**。

---

## 📅 6 月 1 日（周日）下午：各自起跑

### 👨‍💻 A — 6 月 1 日

**全天：官方硬指标核对清单 \+ 开源组件说明启动**

**任务一：输出《官方硬指标核对清单》**
文件路径：`docs/deployment/官方硬指标核对清单.md`

```markdown
# 官方赛题硬指标核对清单
## 一、基础要求
| 序号 | 要求 | 当前状态 | 对应模块 | 负责人 | 验证截图 |
|------|------|:--------:|----------|:------:|----------|
| 1 | ≥6 维学生画像 | ✅/❌ | Profiler Agent | B | |
| 2 | 多智能体协同机制 | ✅/❌ | Orchestrator | A+B | |
| 3 | ≥5 类个性化资源 | ✅/❌ | Resource Generator | B | |
| 4 | 个性化学习路径规划 | ✅/❌ | Path Planner | B | |
| 5 | 防幻觉/可信机制 | ✅/❌ | Trust Mechanism | B | |
| 6 | 自建课程知识库 | ✅/❌ | Chroma 三库 | B | |

## 二、加分项
| 序号 | 加分项 | 当前状态 | 对应模块 |
|------|--------|:--------:|----------|
| 1 | 智能辅导答疑 | ✅/❌ | 多 Agent 对话 |
| 2 | 学习效果评估 | ✅/❌ | 三层诊断引擎 |
| 3 | 流式输出 + 进度追踪 | ✅/❌ | SSE + AgentLog |
| 4 | 离线/弱网方案 | ✅/❌ | Mock 模式（Phase 5） |
| 5 | 协议仿真可视化 | ✅/❌ | SimulatorPlayer |
```

逐项对照实际完成情况填写，未完成的标注负责人和预计完成日期。

**任务二：启动《开源组件与 AI 工具说明》**
文件路径：`docs/deployment/开源组件与AI工具说明.md`
今天完成前两部分：

- 第一部分：后端开源组件清单（FastAPI / Chroma / SQLAlchemy / Pydantic / NetworkX 等，含版本 \+ 协议 \+ 用途）

- 第二部分：前端开源组件清单（Vue3 / ECharts / D3\.js/ TailwindCSS / Prism\.js 等，含版本 \+ 协议 \+ 用途）

**交付检查点**：核对清单提交 dev，开源说明完成前两部分。

---

### 🧠 B — 6 月 1 日

**全天：Diagnosis Agent 第三层（误解模式匹配）\+ 可信机制框架搭建**

**任务一：Diagnosis Agent 第三层实现**
文件：`src/loopse/agents/diagnosis.py`
在已有 surface\_error \+ root\_cause 基础上，接入第三层 `_match_misconception_pattern()`：

```python
# 调用 config/prompts/diagnosis/pattern_match.txt
# 输入：surface_error + root_causes + profile.misconception_patterns
# 输出：MisconceptionPattern 枚举 + 解释 + 干预建议
```

同步更新 `DiagnosisResult` 模型，确保 `misconception_pattern` 字段正常填充。

单元测试：`tests/unit/test_diagnosis_layer3.py`

- 测试用例 1："HTTP 直接跑在 IP 上" → 应返回「层次穿越型」

- 测试用例 2："三次握手就是 SYN、ACK、FIN 三个包" → 应返回「术语混淆型」

- 测试用例 3：正确回答 → 应返回「无明确模式」

**任务二：可信机制 v1 框架搭建**
文件：`src/loopse/core/trust_mechanism.py`
实现 `TrustMechanism` 类基础结构，今天先完成两个核心方法：

1. `verify_scope(generated_content)` — 判断回答是否在「计算机网络」范围内

2. `add_source_annotations(content, sources)` — 给生成内容追加引用来源标注

Prompt 写入 `config/prompts/trust/` 目录下（新建目录）：

- `scope_check.txt`：范围校验 Prompt

- `source_citation.txt`：引用格式化 Prompt

**交付检查点**：Diagnosis 三层全跑通，单元测试 ≥3 条通过；可信机制基础类可 import。

---

### 🎨 C — 6 月 1 日

**全天：剩余 2 类资源卡片（思维导图 \+ 视频脚本）\+ 生成进度追踪设计**

**任务一：MindmapCard 思维导图卡片**
文件：`frontend/src/components/resources/MindmapCard.vue`

- 集成 `mermaid` 渲染思维导图

- 支持错误分支高亮（来自诊断结果的 `targeted_misconception`）

- 卡片样式与 DocCard / ExerciseCard 统一

**任务二：ScriptCard 视频脚本卡片**
文件：`frontend/src/components/resources/ScriptCard.vue`

- 分镜展示：场景描述 \+ 旁白文案 \+ 视觉备注三栏布局

- 支持时长、场景数展示

- 样式与其他资源卡片保持一致

**任务三：ResourceTabBar 组件完善**
文件：`frontend/src/components/resources/ResourceTabBar.vue`
5 个 Tab 全部接通：文档 \| 思维导图 \| 练习题 \| 代码 \| 视频脚本

- 默认展示全部，可按 Tab 筛选

- Tab 上显示该类型资源数量角标

**交付检查点**：5 类资源卡片全部可渲染，Tab 切换正常无报错，提交 dev。

---

## 📅 6 月 2 日（周一）

### 👨‍💻 A — 6 月 2 日

**全天：操作手册初稿 \+ SRS 文档补全 Phase 4 新增内容**

**任务一：《最终用户操作与部署实施手册》初稿**
文件路径：`docs/deployment/操作手册_v1.md`
完成前三章：

- 第一章：系统简介（定位 \+ 核心功能 \+ 适用人群）

- 第二章：环境部署（3 步快速启动 \+ 依赖说明 \+ API Key 配置）

- 第三章：核心功能操作指南

    - 3\.1 对话学习流程（截图占位）

    - 3\.2 画像查看与解读

    - 3\.3 协议仿真播放器使用说明

    - 3\.4 学习路径查看

**任务二：SRS 文档补全可信机制章节**
在 `docs/requirements/SRS_v1.md` 中新增第 2\.9 节：系统可信机制需求

- 知识库溯源要求

- 内容安全过滤要求

- 越界回答拦截规则

- 不确定性声明触发条件

**交付检查点**：操作手册前三章 ≥1500 字，SRS 补全提交。

---

### 🧠 B — 6 月 2 日

**全天：补齐剩余 2 类资源生成（思维导图 \+ 视频脚本）**

**任务一：思维导图生成 Prompt \+ 生成逻辑**
文件：`config/prompts/resource_generator/generate_mindmap.txt`
Prompt 要求：

- 输出 Mermaid 语法的思维导图

- 中心节点 = 知识点名称

- 标注 2\-3 个「常见误解分支」（红色高亮标记）

- 结构不超过 3 层，避免过于复杂

**任务二：视频脚本生成 Prompt \+ 生成逻辑**
文件：`config/prompts/resource_generator/generate_script.txt`
Prompt 要求：

- 输出结构化 JSON：场景列表 \+ 每场景旁白 \+ 视觉备注

- 总时长控制在 60\-90 秒

- 适合作为协议讲解短视频的脚本

**任务三：Resource Generator Agent 统一接入 5 类**
文件：`src/loopse/agents/resource_generator.py`

- `generate()` 方法支持 `resource_types` 参数传入任意组合

- 根据认知风格自动选择生成哪些类型（visual → mindmap \+ script，practical → code \+ exercise）

- 生成结果统一包装为 `BaseResource` 子类列表

单元测试：`tests/unit/test_resource_generator_all_types.py`

- 测试 5 种类型分别生成，返回结构符合 Schema

**交付检查点**：5 类资源全部可通过接口生成，返回格式统一，提交 dev。

---

### 🎨 C — 6 月 2 日

**全天：协议仿真场景扩展（滑动窗口 \+ HTTP 分层封装）**

**任务一：滑动窗口仿真场景**
文件：`src/loopse/agents/simulator_engine.py` 新增场景 `sliding_window`

- 可视化：发送窗口、接收窗口两条动态边界

- 可调参数：窗口大小、报文序号、丢包率

- 步骤：发送 → 到达 → 确认 → 窗口滑动，共 6\-8 步

- 每步附带状态说明：发送窗口左边界、接收窗口位置

**任务二：HTTP 分层封装仿真场景**
新增场景 `http_encapsulation`

- 可视化：左侧应用层 HTTP → 中间传输层 TCP → 右侧网络层 IP → 最下层数据链路层

- 动画：数据从上到下逐层封装，每层添加对应头部

- 高亮：每个包头的关键字段（端口号 / IP 地址 / MAC 地址）

- 配套讲解文字：每一层封装的作用

前端 `SimulatorPlayer.vue` 适配：

- 新增分层堆叠布局（LayerStackPanel 正式启用）

- 支持水平 / 垂直两种布局模式切换

**交付检查点**：仿真场景累计达到 4 个（三次握手、四次挥手、滑动窗口、HTTP 分层），均可正常播放。

---

## 📅 6 月 3 日（周二）

### 👨‍💻 A — 6 月 3 日

**全天：系统开发说明书补全 \+ 架构图文字版细化**

更新 `docs/architecture/系统开发说明书_v1.md`：

- 新增第四章：协议仿真引擎设计（参数化仿真原理 \+ 场景库结构 \+ 前端渲染协议）

- 新增第五章：系统可信机制设计（三层防护架构 \+ 溯源机制 \+ 范围校验）

- 补充第六章：数据库设计（ER 图文字描述 \+ 5 张表字段说明）

同步整理 Agent 协作时序图文字描述：

- 正常问答链路：Orchestrator → Profiler → Retriever → 直接回答

- 诊断链路：Orchestrator → Profiler → Retriever → Diagnosis → Generator → Planner

- 仿真链路：Orchestrator → Retriever → Simulator → Generator

**交付检查点**：开发说明书新增 3 章，提交 dev。

---

### 🧠 B — 6 月 3 日

**全天：基于画像的自适应资源推送逻辑 \+ 可信机制 v1 完整实现**

**任务一：画像驱动的资源推送策略**
文件：`src/loopse/agents/resource_generator.py` 新增 `_select_resource_types()` 方法
推送规则：

```Plain Text
认知风格 = visual → 优先 mindmap + script + doc
认知风格 = practical → 优先 code + exercise + doc
认知风格 = textual → 优先 doc + exercise + mindmap
认知风格 = analogical → 优先 doc + script + exercise

叠加诊断结果：
- 有 flow_omission 错误 → 强制追加 simulator 推荐
- 有 layer_misplacement 错误 → 强制追加 mindmap + http_encapsulation 仿真
- 有 field_misunderstanding 错误 → 强制追加 exercise（报文分析题）
```

**任务二：可信机制 v1 完整实现**
完善 `src/loopse/core/trust_mechanism.py` 的三个核心模块：

1. **范围校验层**：检测回答是否超出计算机网络范畴，越界则返回标准拒答模板

2. **溯源标注层**：将检索到的来源片段编号，在回答末尾追加「参考来源」列表

3. **不确定性声明层**：置信度 \< 0\.6 时，自动追加「以上内容仅供参考」声明

接入 Orchestrator：所有 Generator 生成的内容必须经过 `TrustMechanism.verify_response()` 再返回前端。

**任务三：DNS 解析仿真场景数据**
补充 `simulator_engine.py` 的 `dns_resolution` 场景 JSON 数据

- 递归查询 vs 迭代查询对比展示

- 本地 DNS → 根 → 顶级域 → 权威域 的查询链路

- 共 5\-6 步动画

**交付检查点**：可信机制可拦截越界问题、自动追加引用；资源推送策略生效；DNS 仿真场景数据就绪。

---

### 🎨 C — 6 月 3 日

**全天：生成进度追踪 \+ Agent 日志面板优化 \+ DNS 仿真前端适配**

**任务一：生成进度追踪组件**
文件：`frontend/src/components/resources/GenerationProgress.vue`

- 进度条显示当前生成到第几类资源

- 每完成一类，对应 Tab 角标亮起

- 状态文案："正在生成讲解文档\.\.\." → "正在生成练习题\.\.\." → "全部完成"

- 对接 SSE 事件中的 `resource` 类型事件

**任务二：AgentLogPanel 日志面板优化**
文件：`frontend/src/components/agent/AgentLogPanel.vue`

- 8 个 Agent 头像颜色区分（每个 Agent 一个主题色）

- 日志项展开可查看 input\_state /output\_state 详情

- 支持按 Agent 类型筛选

- 新增耗时显示（duration\_ms 格式化）

- 折叠 / 展开动画过渡

**任务三：DNS 解析仿真前端适配**

- SimulatorPlayer 支持多节点布局（不只是两端通信）

- 树形查询链路渲染：客户端 → 本地 DNS → 根服务器 → TLD 服务器 → 权威服务器

**交付检查点**：进度追踪组件正常显示阶段变化；日志面板支持筛选和详情展开；DNS 仿真可播放。

---

## 📅 6 月 4 日（周三）—— 功能冻结前最后冲刺日

### 👨‍💻 A — 6 月 4 日

**全天：四份文档进度总控 \+ 官方硬指标全量核对**

**任务一：四份文档进度盘点**

- SRS：检查是否覆盖所有功能需求，补全缺失章节

- 开发说明书：检查架构设计是否完整

- 测试说明书：补充 Phase 4 新增测试用例（可信机制测试、5 类资源测试）

- 操作手册：补充协议仿真操作、诊断结果查看两小节

**任务二：官方硬指标逐项全量核对**
更新 `官方硬指标核对清单.md`，每一项附上：

- 功能演示路径

- 对应代码文件位置

- 负责人确认签字（文字标注）

发现缺口当场指派给 B 或 C 当天补齐，不拖到第二天。

**交付检查点**：四份文档均达到 80% 完成度，硬指标清单 95% 以上打勾。

---

### 🧠 B — 6 月 4 日

**全天：可信机制联调 \+ 诊断引擎准确率调优 \+ Mock 数据初版**

**任务一：全链路可信机制联调**

- 对话链路接入：越界问题 → 返回标准拒答 \+ 提示课程范围

- 资源生成链路接入：生成内容末尾自动附带参考来源编号

- 诊断链路接入：诊断置信度低时输出不确定性声明

**任务二：诊断引擎 Prompt 调优**
针对 10 个典型错误场景，调优三层诊断的准确率：

|测试场景|期望错误类型|期望根因|期望模式|
|---|---|---|---|
|HTTP 直接跑在 IP 上|layer\_misplacement|分层封装概念|层次穿越型|
|三次握手两次就够|flow\_omission|连接管理|流程死记型|
|ACK 号 = 序列号|field\_misunderstanding|报文格式|术语混淆型|
|UDP 比 TCP 差|concept\_confusion|协议特性对比|过度简化型|
|Ping 不通就是网络坏了|reasoning\_breakdown|故障排查|局部正确型|

调优结果记录到 `docs/results/diagnosis_accuracy_phase4.md`。

**任务三：启动 Mock 数据准备**
文件：`frontend/public/mock/` 下补齐所有 JSON 的完整结构

- `diagnosis_mock.json`：三层诊断完整示例

- `resources_mock.json`：5 类资源各一个完整示例

- `simulator_mock.json`：三次握手完整步骤数据

格式必须与真实 API 返回 100% 一致，供 Phase 5 离线演示使用。

**交付检查点**：可信机制全链路生效；诊断准确率 ≥80%；Mock 数据结构完整。

---

### 🎨 C — 6 月 4 日

**全天：仿真场景第 5 个（拥塞控制基础版）\+ 前端全组件走查**

**任务一：拥塞控制仿真（简化版）**
新增场景 `congestion_control_basic`

- 可视化：cwnd（拥塞窗口）随时间变化的折线图

- 标注慢启动阶段（指数增长）、拥塞避免阶段（线性增长）

- 丢包事件触发窗口减半

- 只做基础版，精细优化留到 Phase 5

**任务二：前端全组件走查**
逐个页面走查，记录 Bug 到 `docs/results/frontend_bugs_phase4.md`：

- 首页 / 对话页 / 画像页 / 路径页 / 资源页

- 检查 SSE 断线重连、移动端适配、错误提示等边界情况

- 修复发现的 P0/P1 级 Bug

**任务三：路由层接口补全**
后端 `src/loopse/api/` 检查所有路由是否实现：

- `/api/v1/simulator/scenarios` GET

- `/api/v1/simulator/run` POST

- `/api/v1/diagnosis` POST

- `/api/v1/logs/{session_id}` GET

缺失的当天补齐，确保前端调用不 404。

**交付检查点**：仿真场景累计 5 个；前端走查 Bug 清单输出；所有路由接口可用。

---

## 📅 6 月 5 日（周四）—— 三人联调日

### 👨‍💻 A 主持全天联调

**联调流程（按演示脚本走）：**

1. **画像构建链路**：新建会话 → 输入 3\-5 轮对话 → 检查 8 维画像是否更新 → 雷达图是否同步

2. **知识问答链路**：问 "TCP 三次握手过程" → 检查 SSE 流式输出 → 检查引用来源是否附带

3. **诊断链路**：故意说错 "HTTP 直接基于 IP" → 检查三层诊断是否完整返回 → 前端 DiagnosisPanel 是否渲染

4. **仿真链路**：问 "滑动窗口怎么工作" → 检查是否自动触发仿真 → SimulatorPlayer 是否正常播放

5. **资源生成链路**：检查 5 类资源是否全部生成 → ResourceTabBar 切换是否正常 → 进度条是否追踪

6. **路径规划链路**：输入 "我接下来该学什么" → 检查路径节点 \+ 推荐理由 → PathTimeline 展示

7. **可信机制验证**：问 "怎么写 Python 爬虫" → 系统应拒答并说明仅限计算机网络

8. **Agent 日志**：全程检查 AgentLogPanel 是否实时更新 8 个 Agent 调度记录

**联调发现的问题当场分配修复，A 记录到 ****`docs/results/phase4_integration_issues.md`****。**

---

## 📅 6 月 6 日（周五）—— 查漏补缺日

### 三人各自负责修复联调发现的问题

- **A**：文档补全 \+ 硬指标最终核对 \+ 开源组件说明定稿

- **B**：修复诊断 / 生成 / 可信机制相关 Bug \+ Mock 数据完善

- **C**：修复前端渲染、仿真播放、接口对接相关 Bug \+ 优化 UI 细节

**下午 A 组织二次走查**，确保所有 P0 级问题清零，P1 问题有明确修复计划。

---

## 📅 6 月 7 日（周六）—— Phase 4 验收日

### 上午：A 主持正式验收，逐项过验收标准

```Plain Text
Phase 4 验收清单：
[ ] 5 类资源全部可稳定生成（文档/思维导图/练习题/代码/视频脚本）
[ ] Diagnosis 三层诊断完整运行：表面错误 + 根因溯源 + 误解模式匹配
[ ] 协议仿真场景 ≥ 5 个（握手/挥手/滑动窗口/HTTP 分层/DNS/拥塞控制基础）
[ ] 系统可信机制：越界拦截正常 + 引用溯源正常 + 不确定性声明正常
[ ] 生成进度追踪：前端可显示资源生成阶段变化
[ ] Agent 日志面板：支持筛选、详情展开、耗时显示
[ ] 官方硬指标：基础要求 6 项 100% 打勾
[ ] 加分项：智能辅导、效果评估、流式输出 3 项可演示
[ ] 四份文档：均达到 80% 以上完成度
[ ] Mock 数据：5 个 JSON 结构完整，与真实接口一致
[ ] 所有路由接口：前端调用无 404
```

### 下午：Phase 5 预热

A 输出 Phase 5 核心任务预告框架（三大创新点攻坚 \+ 6 月 14 日 Code Freeze），三人各自梳理 Phase 5 需要的前置准备。

---

## 📋 Phase 4 总交付物一览

|交付物|负责人|路径|
|---|---|---|
|官方硬指标核对清单|A|`docs/deployment/官方硬指标核对清单.md`|
|开源组件与 AI 工具说明|A|`docs/deployment/开源组件与AI工具说明.md`|
|操作手册初稿|A|`docs/deployment/操作手册_v1.md`|
|SRS 补全（可信机制章节）|A|`docs/requirements/SRS_v1.md`|
|开发说明书补全（仿真 \+ 可信 \+ 数据库）|A|`docs/architecture/系统开发说明书_v1.md`|
|Diagnosis 第三层（模式匹配）|B|`src/loopse/agents/diagnosis.py`|
|5 类资源生成全量|B|`src/loopse/agents/resource_generator.py` \+ 5 个 Prompt|
|画像驱动资源推送策略|B|`resource_generator.py` 新增方法|
|系统可信机制 v1|B|`src/loopse/core/trust_mechanism.py`|
|诊断准确率调优报告|B|`docs/results/diagnosis_accuracy_phase4.md`|
|Mock 数据（5 类完整 JSON）|B\+C|`frontend/public/mock/*.json`|
|MindmapCard \+ ScriptCard|C|`frontend/src/components/resources/`|
|ResourceTabBar 五 Tab 全通|C|`frontend/src/components/resources/ResourceTabBar.vue`|
|仿真扩展至 5\+ 场景|C|`SimulatorPlayer.vue` \+ 场景数据|
|生成进度追踪组件|C|`GenerationProgress.vue`|
|AgentLogPanel 优化版|C|`AgentLogPanel.vue`|
|所有路由接口补齐|C|`src/loopse/api/*.py`|

---

> **Phase 4 结束标志**：官方硬指标全部打勾，系统具备完整演示能力，进入 Phase 5 的创新点深度打磨阶段。6 月 14 日 Code Freeze 前，所有功能必须在此阶段内定型，Phase 5 只做优化不做新增。
> 
> 

> （注：部分内容可能由 AI 生成）
