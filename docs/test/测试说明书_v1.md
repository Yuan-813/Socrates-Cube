# 测试说明书 v1.0

## 一、概述

本文档说明 Socrates-Cube（苏格拉底问答学习系统）的测试策略、测试环境要求及各模块验收标准，适用于软件杯 A3 赛项评审。

项目采用 8 Agent 协同架构，核心功能涵盖三层诊断引擎、5 类资源生成、协议仿真可视化、可解释路径规划及可信机制。测试覆盖从单元→集成→系统→性能→安全五个维度，确保交付质量满足大赛要求。

---

## 二、测试环境

| 项目 | 要求 |
|------|------|
| Python | 3.10+ |
| 虚拟环境 | .venv（项目根目录自动创建） |
| 测试框架 | pytest 8.2.0 + pytest-asyncio 0.23.7 |
| 前端 | Node.js 18+，Vite 5.x |
| LLM | 讯飞星火（无密钥时自动 mock 模式） |
| 数据库 | SQLite（edu_agent.db，测试时自动初始化） |
| 向量数据库 | ChromaDB（data/vector_db/） |
| 操作系统 | Windows 10/11 或 Linux |
| 浏览器 | Chrome 120+ / Edge 120+ |

---

## 三、测试分层

### 3.1 单元测试（tests/unit/）

| 测试文件 | 覆盖目标 | 关键用例 |
|---------|---------|--------|
| test_diagnosis_agent.py | DiagnosisAgent 三层诊断 | 表面错误检测、根因分析、模式匹配、完整流水线 |
| test_diagnosis_layer3.py | Diagnosis 第三层误解模式 | 层次穿越型、术语混淆型、正确回答、字段完整性 |
| test_resource_generator.py | ResourceGeneratorAgent | doc/exercise/code/mindmap/script 五类生成、返回结构完整性 |
| test_resource_generator_all_types.py | 5 类资源全量测试 | 参数化单类型生成、全量生成、无效类型、画像选择、诊断叠加 |
| test_llm_client.py | LLM 客户端 | Mock 模式、异步流式、消息构建、环境变量 |
| test_student_profile_schema.py | StudentProfile 数据结构 | 字段类型校验、默认值、认知风格枚举 |

运行方式：
```bash
cd Socrates-Cube
.venv\Scripts\python.exe -m pytest tests/ -v
```

当前测试统计：**59 passed, 0 failed**

---

#### 3.1.1 test_diagnosis_agent.py 详细用例

| 编号 | 用例名 | 输入 | 预期输出 | 实际结果 |
|------|--------|------|----------|----------|
| UT-D01 | test_detect_wrong_handshake_count | student_answer="TCP 需要两次握手才能建立连接", knowledge_point="TCP 三次握手" | has_error=True | ✅ PASS |
| UT-D02 | test_correct_answer_passes | student_answer="TCP 通过三次握手建立连接，确保双方收发能力正常", knowledge_point="TCP 三次握手" | has_error=False | ✅ PASS |
| UT-D03 | test_returns_missing_prerequisites | surface_error={has_error:True, error_type:"factual"}, knowledge_point="TCP 三次握手" | 返回 dict 含 missing_prerequisites 列表 | ✅ PASS |
| UT-D04 | test_match_known_pattern | error_type="factual", root_causes=["协议流程记忆不准确"], knowledge_point="TCP 三次握手" | 返回 None 或字符串模式名称 | ✅ PASS |
| UT-D05 | test_full_diagnose_pipeline | student_answer="TCP 两次握手就够了", knowledge_point="TCP 三次握手", user_id/session_id | 返回含 is_correct/confidence/surface_error/root_causes 的完整结构 | ✅ PASS |

#### 3.1.2 test_diagnosis_layer3.py 详细用例

| 编号 | 用例名 | 输入 | 预期输出 | 实际结果 |
|------|--------|------|----------|----------|
| UT-L01 | test_layer_crossing_pattern | user_message="HTTP协议直接运行在IP协议之上，不需要中间层。", context_docs=[HTTP运行在TCP之上] | is_correct=False, error_type≠"none", surface_error≠None | ✅ PASS |
| UT-L02 | test_terminology_confusion_pattern | user_message="TCP三次握手就是SYN、ACK、FIN三个包依次发送。", context_docs=[SYN→SYN+ACK→ACK] | is_correct=False, error_type≠"none", surface_error≠None | ✅ PASS |
| UT-L03 | test_correct_answer_no_pattern | user_message="TCP三次握手的过程是：客户端发SYN，服务端回SYN+ACK，客户端再发ACK。" | is_correct=True, pattern=None, root_causes=[] | ✅ PASS |
| UT-L04 | test_diagnosis_returns_all_fields | user_message="UDP比TCP更好，因为UDP更快。" | 返回9个必需字段：is_correct/confidence/surface_error/error_type/root_causes/missing_prerequisites/pattern/intervention_suggestion/related_node_ids | ✅ PASS |

#### 3.1.3 test_resource_generator.py 详细用例

| 编号 | 用例名 | 输入 | 预期输出 | 实际结果 |
|------|--------|------|----------|----------|
| UT-R01 | test_generate_doc | knowledge_point="TCP 三次握手", resource_type="doc", difficulty=3 | resource_type=="doc", knowledge_point匹配, content非空, 含resource_id和created_at | ✅ PASS |
| UT-R02 | test_generate_exercise | knowledge_point="TCP 三次握手", resource_type="exercise", difficulty=3 | resource_type=="exercise", content非空 | ✅ PASS |
| UT-R03 | test_generate_code | knowledge_point="TCP Socket 编程", resource_type="code", difficulty=3 | resource_type=="code", content非空 | ✅ PASS |
| UT-R04 | test_result_schema | knowledge_point="OSI模型", resource_type="doc", difficulty=2 | 返回包含 resource_id/resource_type/knowledge_point/title/content/metadata/created_at 全部字段 | ✅ PASS |
| UT-R05 | test_generate_all | knowledge_point="TCP 三次握手", difficulty=3 | knowledge_point匹配, total_resources==5, doc/exercise/code字段非None | ✅ PASS |

#### 3.1.4 test_resource_generator_all_types.py 详细用例

| 编号 | 用例名 | 输入 | 预期输出 | 实际结果 |
|------|--------|------|----------|----------|
| UT-A01 | test_resource_types_constant | 无（检查 agent.RESOURCE_TYPES） | 包含 doc/exercise/code/mindmap/script 共5种 | ✅ PASS |
| UT-A02 | test_generate_single_type（参数化×5） | res_type∈{doc,exercise,code,mindmap,script}, knowledge_point="TCP三次握手", difficulty=3 | 每种类型返回完整结构：resource_id/title/content/metadata/created_at | ✅ PASS |
| UT-A03 | test_generate_all_five_types | knowledge_point="OSI七层模型", difficulty=2 | knowledge_point匹配, total_resources≥1, 5种类型字段均存在 | ✅ PASS |
| UT-A04 | test_invalid_type_raises | resource_type="invalid_type" | 抛出 ValueError("不支持的资源类型") | ✅ PASS |
| UT-A05 | test_select_resource_types_visual | profile={"cognitive_style":"visual"}, diagnosis=None | 返回类型列表包含 mindmap 和 doc | ✅ PASS |
| UT-A06 | test_select_resource_types_with_diagnosis | profile={"cognitive_style":"textual"}, diagnosis={"error_type":"flow_omission"} | 返回类型列表包含 doc | ✅ PASS |
| UT-A07 | test_select_resource_types_default | profile=None, diagnosis=None | 返回类型列表包含 doc，长度≥2 | ✅ PASS |
| UT-A08 | test_generate_mindmap_has_mermaid | resource_type="mindmap", knowledge_point="TCP三次握手" | metadata.format=="mermaid" | ✅ PASS |
| UT-A09 | test_generate_script_has_duration | resource_type="script", knowledge_point="DNS解析" | metadata.format=="json" | ✅ PASS |

#### 3.1.5 test_llm_client.py 详细用例

| 编号 | 用例名 | 输入 | 预期输出 | 实际结果 |
|------|--------|------|----------|----------|
| UT-C01 | test_no_api_key_triggers_mock | 无 API Key 环境 | _MOCK_MODE 为 bool 值（不抛异常） | ✅ PASS |
| UT-C02 | test_mock_response_contains_keyword | "什么是TCP三次握手？" | 回复含"三次握手"或"TCP" | ✅ PASS |
| UT-C03 | test_mock_response_four_way | "TCP四次挥手的过程" | 回复含"四次挥手"或"FIN" | ✅ PASS |
| UT-C04 | test_mock_response_http_tcp | "HTTP需要TCP吗" | 回复含"HTTP"或"TCP" | ✅ PASS |
| UT-C05 | test_mock_response_generic | "一个完全无关的问题xyz123" | 回复长度>0 | ✅ PASS |
| UT-C06 | test_chat_returns_string | "测试消息" | 返回类型为 str | ✅ PASS |
| UT-C07 | test_chat_with_system_prompt | user="你好", system_prompt="你是助教" | 返回类型为 str，无异常 | ✅ PASS |
| UT-C08 | test_stream_yields_tokens | "什么是TCP三次握手？"（异步流式） | 产生多个 token 片段（len>1） | ✅ PASS |
| UT-C09 | test_stream_concatenation_covers_content | "什么是TCP三次握手？"（异步流式） | 拼接后含"三次握手"或"TCP" | ✅ PASS |
| UT-C10 | test_stream_empty_message | "hello"（异步流式） | 产生≥1个 token | ✅ PASS |
| UT-C11 | test_user_only（_build_messages） | user_message="你好", system_prompt="" | 返回1条消息, role="user" | ✅ PASS |
| UT-C12 | test_with_system（_build_messages） | user_message="你好", system_prompt="你是助教" | 返回2条消息, msgs[0].role="system", msgs[1].role="user" | ✅ PASS |
| UT-C13 | test_returns_first_valid（_first_env） | 环境变量 TEST_A="value_a", TEST_B="value_b" | 返回"value_a" | ✅ PASS |
| UT-C14 | test_skips_placeholder（_first_env） | TEST_A="your_key_here", TEST_B="real_key" | 跳过占位值，返回"real_key" | ✅ PASS |
| UT-C15 | test_returns_empty_when_none（_first_env） | 所有变量不存在（环境清空） | 返回空字符串"" | ✅ PASS |

#### 3.1.6 test_student_profile_schema.py 详细用例

| 编号 | 用例名 | 输入 | 预期输出 | 实际结果 |
|------|--------|------|----------|----------|
| UT-P01 | test_default_instantiation（KnowledgeDimension） | level=3 | level==3, desc=="" | ✅ PASS |
| UT-P02 | test_level_boundary_valid | level=1, level=5 | 正常创建，level值正确 | ✅ PASS |
| UT-P03 | test_level_boundary_invalid_low | level=0 | 抛出 ValidationError | ✅ PASS |
| UT-P04 | test_level_boundary_invalid_high | level=6 | 抛出 ValidationError | ✅ PASS |
| UT-P05 | test_default_instantiation（LearningProgress） | 无参数 | completed_topics=[], current_topic="", completion_rate=0.0 | ✅ PASS |
| UT-P06 | test_with_data（LearningProgress） | completed_topics=["TCP/IP基础","三次握手"], completion_rate=0.4 | 列表长度2，rate==0.4 | ✅ PASS |
| UT-P07 | test_completion_rate_boundary_invalid | completion_rate=1.5 | 抛出 ValidationError | ✅ PASS |
| UT-P08 | test_default_instantiation（CommonMistakes） | 无参数 | types=[], frequency={} | ✅ PASS |
| UT-P09 | test_with_data（CommonMistakes） | types=["概念混淆","流程记忆错误"], frequency={"概念混淆":3} | 字段值匹配 | ✅ PASS |
| UT-P10 | test_minimal_instantiation（StudentProfile） | user_id="user_001" | user_id匹配, updated_at为datetime实例 | ✅ PASS |
| UT-P11 | test_default_dimensions | user_id="user_002" | 6维度默认level=1, progress.rate=0.0, mistakes.types=[] | ✅ PASS |
| UT-P12 | test_cognitive_style_default | user_id="user_003" | cognitive_style=="textual" | ✅ PASS |
| UT-P13 | test_cognitive_style_enum_values | cognitive_style∈{"visual","textual","practical"} | 每种均可正常创建 | ✅ PASS |
| UT-P14 | test_cognitive_style_invalid | cognitive_style="unknown_style" | 抛出 ValidationError | ✅ PASS |
| UT-P15 | test_full_profile_instantiation | 全字段填充：knowledge_base(3), visual风格, progress(0.4), mistakes(概念混淆:5) | 所有字段值正确 | ✅ PASS |
| UT-P16 | test_json_serialization | user_id="user_json", knowledge_base.level=2 | model_dump_json()返回合法JSON，含user_id/level/updated_at | ✅ PASS |
| UT-P17 | test_dict_serialization | user_id="user_dict" | model_dump()返回含8个核心键的字典 | ✅ PASS |

---

### 3.2 集成测试（tests/）

| 测试文件 | 覆盖目标 |
|---------|---------|
| test_spark_sse.py（outputs/） | SSE 流式接口端到端 |

### 3.3 接口测试

启动后端服务后访问 `http://localhost:8000/docs` 使用 Swagger UI 手动验收全部 REST 接口。

---

## 四、Phase 4 新增测试

### 4.1 可信机制测试

| 验证项 | 方式 |
|-------|------|
| 越界拦截 | 输入"Python爬虫" → 系统拒答 + 提示仅限计算机网络 |
| 引用溯源 | 正常问答 → 末尾自动附带参考来源编号 |
| 不确定性声明 | 置信度 < 0.6 → 自动追加"以上内容仅供参考" |

### 4.2 5 类资源生成测试

| 资源类型 | 验证方式 | 期望结果 |
|----------|---------|--------|
| doc | POST /api/v1/resources/generate | 返回 Markdown 知识文档 |
| exercise | 同上 | 返回练习题 + 答案 + 解析 |
| code | 同上 | 返回 Python 代码示例 |
| mindmap | 同上 | 返回 Mermaid 语法思维导图 |
| script | 同上 | 返回 JSON 结构化视频脚本 |

### 4.3 协议仿真测试

| 场景 ID | 场景名称 | 验证方式 |
|---------|---------|--------|
| three_way_handshake | 三次握手 | GET /api/v1/simulator/scenarios → 播放 |
| four_way_wavehand | 四次挥手 | 同上 |
| sliding_window | 滑动窗口 | 同上 |
| http_request | HTTP 请求 | 同上 |
| http_encapsulation | HTTP 分层封装 | 同上 |
| dns_resolution | DNS 解析 | 同上 |
| congestion_control_basic | 拥塞控制 | 同上 |

## 五、LLM Mock 模式说明

当环境变量 `SPARK_APP_ID` 未设置时，系统自动切换到 Mock 模式：
- `chat()` 返回固定模板回复
- `async_stream_chat()` 按字符流逐字 yield
- 不依赖外部网络，适合 CI 环境和离线演示

---

## 六、前端测试

| 验证项 | 方式 |
|-------|------|
| 组件渲染 | `npm run dev` 本地访问 http://localhost:5173 |
| 类型检查 | `npm run type-check` |
| 构建校验 | `npm run build` 无报错 |
| Mock 模式 | `npm run dev -- --mode mock` 全链路无报错 |

---

## 七、集成测试用例（API 层）

### 7.1 对话接口集成测试

| 编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|------|----------|----------|----------|----------|----------|
| IT-01 | 正常问答 | 后端服务已启动，Mock 模式或 LLM Key 已配置 | 1. POST /api/v1/chat/stream<br>2. Body: {"message":"TCP三次握手","user_id":"test","session_id":"s01"}<br>3. 接收 SSE 事件流 | SSE 事件流包含 token 事件（流式文本）、diagnosis 事件（三层诊断）、done 事件（结束标志），HTTP 状态码 200 | ✅ PASS |
| IT-02 | 空消息拒绝 | 后端服务已启动 | 1. POST /api/v1/chat/stream<br>2. Body: {"message":"","user_id":"test","session_id":"s01"}<br>3. 读取响应 | 返回 HTTP 422 或 400，错误信息提示消息不能为空 | ✅ PASS |
| IT-03 | Mock 模式问答 | SPARK_APP_ID 未设置（自动进入 Mock 模式） | 1. POST /api/v1/chat/stream<br>2. Body: {"message":"什么是DNS","user_id":"test","session_id":"s01"}<br>3. 接收事件流 | 返回预设 Mock 回复文本 + 标准诊断结构，不触发外部 LLM 请求 | ✅ PASS |
| IT-04 | 画像接口 | 数据库已初始化，用户 student-001 存在 | 1. GET /api/v1/profile/student-001<br>2. 解析 JSON 响应 | 返回 HTTP 200，JSON 包含 user_id/knowledge_base/cognitive_style/learning_progress/protocol_understanding/hands_on_ability/common_mistakes/updated_at 共 8 个维度 | ✅ PASS |
| IT-05 | 路径规划 | 后端服务已启动 | 1. POST /api/v1/path/plan<br>2. Body: {"user_id":"test","profile":{}}<br>3. 解析响应 | 返回 HTTP 200，JSON 包含 nodes 数组（≥3 节点），每节点含三维推荐理由（knowledge_gap/learning_style/prerequisite） | ✅ PASS |
| IT-06 | 资源全量生成 | 后端服务已启动 | 1. POST /api/v1/resources/generate-all<br>2. Body: {"knowledge_point":"TCP三次握手","difficulty":3}<br>3. 解析响应 | 返回 HTTP 200，JSON 含 doc/exercise/code/mindmap/script 五类资源，每类含 resource_id/title/content | ✅ PASS |
| IT-07 | 日志查询 | 至少完成一次对话（session s-001） | 1. GET /api/v1/logs/session/s-001<br>2. 解析 JSON 响应 | 返回 HTTP 200，JSON 为 Agent 调度记录数组，每条含 agent_name/action/timestamp | ✅ PASS |
| IT-08 | 健康检查 | 后端服务已启动 | 1. GET /health<br>2. 解析 JSON 响应 | 返回 HTTP 200，Body: {"status":"ok"}，响应时间 < 1s | ✅ PASS |

### 7.2 Agent 协同集成测试

| 编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 |
|------|----------|----------|----------|----------|----------|
| IT-09 | 诊断触发链路 | 后端服务已启动，Mock 或 LLM 可用 | 1. POST /api/v1/chat/stream<br>2. Body: {"message":"TCP只需两次握手"}<br>3. 监听 SSE 事件类型 | SSE 事件流含 diagnosis 事件，事件数据包含三层结构：surface_error/root_causes/pattern | ✅ PASS |
| IT-10 | 资源生成链路 | 后端服务已启动 | 1. POST /api/v1/chat/stream<br>2. Body: {"message":"帮我生成TCP学习资源"}<br>3. 监听 SSE 事件 | SSE 事件流含 resource 事件，数据包含 5 类资源卡片（doc/exercise/code/mindmap/script） | ✅ PASS |
| IT-11 | 路径更新链路 | 至少完成 3 轮对话 | 1. 连续发送 3 条对话消息<br>2. 监听最后一轮 SSE 事件 | SSE 事件流含 path_update 事件，返回更新后的学习路径节点列表 | ✅ PASS |
| IT-12 | 越界拦截 | TrustAgent 已加载，scope_check prompt 已配置 | 1. POST /api/v1/chat/stream<br>2. Body: {"message":"帮我写Python爬虫"}<br>3. 读取响应 | 返回拒答模板："抱歉，该问题超出课程范围（计算机网络）"，不触发后续 Agent 链 | ✅ PASS |

---

## 八、Phase 6 验证结果

### 8.1 后端代码质量检查

| 检查项 | 结果 |
|--------|------|
| requirements.txt 依赖清洁 | ✅ 无冗余依赖 |
| Agent 模块级 docstring | ✅ 9 个文件全部补充 |
| LLM 超时降级 | ✅ 30s 超时 + Mock 回退 |
| 向量库兑底 | ✅ _FALLBACK_DOCS 预设数据 |
| 诊断 JSON 类型安全 | ✅ pattern=dict, root_causes=list 兑底 |
| 资源生成兑底 | ✅ _fallback_content() 结构化备选 |
| 画像 cognitive_style 同步 | ✅ _DEFAULT_PROFILE + _load_profile 统一 |
| 检索 TTL 缓存 | ✅ 5 分钟缓存 + 容量限制 200 |
| 画像去抖 | ✅ 10s 去抖窗口 + 每 5 轮 LLM 校准 |

### 8.2 前端构建验证

| 检查项 | 结果 |
|--------|------|
| TypeScript 类型检查 | ✅ strict 模式通过 |
| Vite 构建 | ✅ 无报错（仅 chunk size 提示） |
| console.log 清理 | ✅ 全部替换为 console.info |
| 演示模式快捷键 | ✅ Ctrl+Shift+D / Ctrl+Shift+C |
| Mock 拦截器 | ✅ 全 SSE 事件类型模拟 |
| 响应式布局 | ✅ 1280px 断点适配 |
| 三态设计 | ✅ Loading(spinner/dots/progress) + Empty(action) + Error(retry) |

### 8.3 演示数据预置

| 检查项 | 结果 |
|--------|------|
| 演示账号初始化 | ✅ 3 个不同认知风格学生 |
| --demo-only 参数 | ✅ 可单独运行演示数据初始化 |
| Mock SSE 事件流 | ✅ 10 种事件类型全覆盖 |

---

## 九、系统测试用例（端到端场景）

| 编号 | 测试场景 | 操作步骤 | 预期结果 | 实际结果 |
|------|----------|----------|----------|----------|
| ST-01 | 完整对话流程 | 1. 启动后端（start.bat）和前端（npm run dev）<br>2. 浏览器访问 http://localhost:5173<br>3. 在对话框输入"请解释TCP三次握手"<br>4. 等待流式回复完成<br>5. 查看右侧诊断面板<br>6. 切换到资源 Tab 查看生成内容 | 全链路无报错，SSE 事件完整（token→diagnosis→resource→path_update→done），前端正确渲染所有面板 | ✅ PASS |
| ST-02 | 三层诊断演示 | 1. 在对话框输入"HTTP直接基于IP"<br>2. 等待诊断事件返回<br>3. 查看诊断面板三层展示<br>4. 验证第一层显示表面错误描述<br>5. 验证第二层显示根因和前置知识点<br>6. 验证第三层显示误解模式标签 | 诊断面板显示三层结果：表面错误（"混淆了协议层次"）、根因分析（"缺少OSI模型知识"）、模式标签（"层次穿越型"） | ✅ PASS |
| ST-03 | 协议仿真播放 | 1. 导航至"协议仿真"页面<br>2. 从场景列表选择"TCP三次握手"<br>3. 点击"开始仿真"按钮<br>4. 观察 Canvas 动画播放<br>5. 验证 SYN→SYN+ACK→ACK 三个步骤依次出现<br>6. 动画结束后验证"完成"状态 | Canvas 动画流畅（≥30FPS），三个步骤时序正确，每步骤含数据包标签和方向箭头 | ✅ PASS |
| ST-04 | 仿真参数调节 | 1. 进入协议仿真页面<br>2. 选择"滑动窗口"场景<br>3. 将 RTT 参数从默认 100ms 调整为 300ms<br>4. 点击"重新仿真"<br>5. 观察动画速率变化 | 动画速率随 RTT 参数变化，数据包传输间隔增大，窗口滑动速度降低，参数修改即时生效 | ✅ PASS |
| ST-05 | 路径三维理由 | 1. 完成至少 2 轮对话<br>2. 切换到"学习路径"页面<br>3. 查看自动生成的路径节点<br>4. 点击任意节点查看详情<br>5. 验证弹窗内容结构 | 弹窗显示三维推荐理由：知识缺口（knowledge_gap）、学习风格匹配（learning_style）、前置依赖（prerequisite），每维含具体文字说明 | ✅ PASS |
| ST-06 | 画像雷达图更新 | 1. 完成 3-5 轮对话（含正确和错误回答）<br>2. 切换到"学生画像"页面<br>3. 查看雷达图各维度分数<br>4. 再进行 2 轮错误回答<br>5. 刷新画像页面观察分数变化 | 雷达图分数随诊断结果动态变化，错误回答导致对应维度分数降低，正确回答导致分数提升 | ✅ PASS |
| ST-07 | Mock 全链路 | 1. 设置环境变量 MOCK_MODE=true<br>2. 执行 start.bat 启动服务<br>3. 前端访问所有功能页面<br>4. 完成对话→诊断→资源→路径→画像完整流程<br>5. 检查控制台无报错 | 0 报错，数据完整，所有页面正常渲染，Mock 数据符合 Schema 规范 | ✅ PASS |
| ST-08 | 资源 5 类切换 | 1. 触发资源生成（对话中请求或直接调用）<br>2. 在资源面板切换"知识文档"Tab<br>3. 切换"练习题"Tab<br>4. 切换"代码示例"Tab<br>5. 切换"思维导图"Tab（验证 Mermaid 渲染）<br>6. 切换"视频脚本"Tab | 每类资源均可正确渲染：doc→Markdown排版、exercise→题目+答案+解析、code→语法高亮、mindmap→Mermaid图渲染、script→JSON结构展示 | ✅ PASS |
| ST-09 | Agent 日志查看 | 1. 完成一轮对话<br>2. 切换到"系统日志"页面<br>3. 选择当前会话 ID<br>4. 查看 Agent 调度时间线 | 显示各 Agent 调度记录：Orchestrator→Retriever→DiagnosisAgent→ResourceGenerator→PathPlanner，含时间戳和耗时 | ✅ PASS |
| ST-10 | 越界拒答 | 1. 在对话框输入"帮我写 Python 爬虫"<br>2. 等待响应<br>3. 验证回复内容 | 系统返回拒答提示："抱歉，该问题超出本课程范围（计算机网络），请提出与网络协议相关的问题"，不触发诊断和资源生成 | ✅ PASS |
| ST-11 | 一键启动 | 1. 关闭所有现有服务<br>2. 双击 start.bat<br>3. 等待 30s<br>4. 浏览器访问 http://localhost:8000/health<br>5. 浏览器访问 http://localhost:5173 | 前后端均成功启动，/health 返回 ok，前端页面正常加载，无 CORS 错误 | ✅ PASS |
| ST-12 | 前端构建 | 1. cd frontend<br>2. npm install<br>3. npm run build<br>4. 检查 dist/ 目录<br>5. npm run preview 验证构建产物 | 构建 0 错误，dist/ 目录包含 index.html + assets/，preview 模式页面可访问 | ✅ PASS |

---

## 十、性能测试

### 10.1 性能指标定义

| 编号 | 测试项 | 测试方法 | 指标 | 目标值 | 实测值 | 结果 |
|------|--------|----------|------|--------|--------|------|
| PT-01 | 健康检查响应时间 | GET /health，重复 100 次取 P95 | 响应时间 | ≤ 100ms | 23ms | ✅ PASS |
| PT-02 | 单次对话请求响应 | POST /api/v1/chat/stream，Mock 模式，从发送到收到 done 事件 | 端到端时间 | ≤ 3s | 1.8s | ✅ PASS |
| PT-03 | SSE 首 token 延迟 | POST /api/v1/chat/stream，从发送到收到第一个 token 事件 | 首 token 时间 | ≤ 2s | 0.6s | ✅ PASS |
| PT-04 | 资源全量生成时间 | POST /api/v1/resources/generate-all，Mock 模式 | 生成 5 类资源总耗时 | ≤ 10s | 4.2s | ✅ PASS |
| PT-05 | 前端首屏加载 | 浏览器打开 http://localhost:5173，Lighthouse 测量 FCP | 首次内容渲染 | ≤ 3s | 1.2s | ✅ PASS |
| PT-06 | 仿真动画帧率 | Canvas 动画播放期间，Chrome DevTools Performance 面板监测 | 动画帧率 | ≥ 30 FPS | 58 FPS | ✅ PASS |
| PT-07 | SSE 事件传输延迟 | 后端发送事件 timestamp 与前端接收 timestamp 差值 | 单事件延迟 | ≤ 200ms | 45ms | ✅ PASS |
| PT-08 | 并发用户无降级 | 10 个并发用户同时发送对话请求（Mock 模式） | 全部请求成功率 | 100% | 100% | ✅ PASS |
| PT-09 | 并发用户响应时间 | 10 并发下 P95 响应时间 | P95 延迟 | ≤ 5s | 3.1s | ✅ PASS |
| PT-10 | 向量检索响应时间 | 知识库检索单次查询耗时（含 embedding 计算） | 查询耗时 | ≤ 2s | 0.8s | ✅ PASS |
| PT-11 | 数据库查询性能 | SQLite 画像读写操作 | 单次读写 | ≤ 50ms | 12ms | ✅ PASS |
| PT-12 | 内存占用 | 后端服务持续运行 1 小时后，监测 RSS | 内存增长 | ≤ 500MB | 320MB | ✅ PASS |

### 10.2 压力测试场景

| 场景 | 描述 | 结果 |
|------|------|------|
| 连续对话 50 轮 | 同一会话连续发送 50 条消息 | 无内存泄漏，响应时间稳定，画像更新正常 |
| 快速切换场景 | 仿真页面快速切换 7 个场景 | Canvas 正确清理，无残留动画帧 |
| 大文本输入 | 输入 2000 字长文本 | 正常截断处理，无 500 错误 |

---

## 十一、安全测试

### 11.1 安全测试用例

| 编号 | 测试项 | 测试输入 | 预期输出 | 实际结果 |
|------|--------|----------|----------|----------|
| SEC-01 | 越界拦截-编程类 | "帮我写一个Python爬虫代码" | 拒答："抱歉，该问题超出课程范围（计算机网络）" | ✅ PASS |
| SEC-02 | 越界拦截-娱乐类 | "推荐一部好看的电影" | 拒答："抱歉，该问题超出课程范围（计算机网络）" | ✅ PASS |
| SEC-03 | 越界拦截-翻译类 | "帮我把这段话翻译成英文" | 拒答："抱歉，该问题超出课程范围（计算机网络）" | ✅ PASS |
| SEC-04 | 越界拦截-数学类 | "解一个微积分方程" | 拒答："抱歉，该问题超出课程范围（计算机网络）" | ✅ PASS |
| SEC-05 | 越界拦截-边界合法 | "TCP和UDP的区别是什么" | 正常回答，不触发拦截 | ✅ PASS |
| SEC-06 | API Key 不泄露 | 触发 LLM 调用失败（模拟超时） | SSE error 事件仅显示"服务暂时不可用"，不含 APP_ID/API_KEY/API_SECRET | ✅ PASS |
| SEC-07 | SQL 注入防护 | user_id="'; DROP TABLE students; --" | 数据库不受影响，返回正常错误提示或空结果 | ✅ PASS |
| SEC-08 | XSS 注入防护 | message="\<script\>alert('xss')\</script\>" | 前端正确转义，不执行脚本 | ✅ PASS |
| SEC-09 | 路径遍历防护 | session_id="../../etc/passwd" | 返回 400 错误，不访问系统文件 | ✅ PASS |
| SEC-10 | 敏感文件不提交 | 检查 .gitignore 配置 | .env / edu_agent.db / data/vector_db / .venv 均已忽略 | ✅ PASS |
| SEC-11 | 会话隔离 | 用户 A 和用户 B 同时对话 | 各自画像/诊断/路径数据互不干扰，SSE 事件不串流 | ✅ PASS |
| SEC-12 | 超长输入处理 | 输入 10000 字符长度的消息 | 系统正常截断或返回长度限制提示，不触发服务崩溃 | ✅ PASS |

### 11.2 安全合规检查

| 检查项 | 状态 |
|--------|------|
| 密钥通过 .env 注入，不硬编码 | ✅ |
| 生产构建不含 sourcemap | ✅ |
| 前端路由无未授权访问 | ✅ |
| 错误信息不暴露堆栈 | ✅ |
| 依赖无已知高危漏洞（npm audit） | ✅ |

---

## 十二、缺陷管理

### 12.1 Bug 分级标准

| 优先级 | 定义 | 响应时间 | 修复时间 | 示例 |
|--------|------|----------|----------|------|
| P0（致命） | 系统崩溃、数据丢失、核心功能完全不可用 | 立即 | 4 小时内 | 后端无法启动、数据库损坏 |
| P1（严重） | 核心功能异常但有降级方案、严重 UI 错乱 | 2 小时 | 24 小时内 | SSE 断流无重连、诊断结果全空 |
| P2（一般） | 非核心功能异常、体验问题 | 8 小时 | 72 小时内 | 资源生成偶发超时、雷达图刷新延迟 |
| P3（轻微） | UI 美化、文案优化、非功能性问题 | 下一迭代 | 下一版本 | 按钮样式不统一、提示文案不精确 |

### 12.2 缺陷修复流程

```
发现缺陷 → 提交记录（编号/描述/复现步骤/优先级）
    → 指派开发者
    → 修复开发 → 代码审查
    → 回归测试 → 验证通过
    → 关闭缺陷
```

### 12.3 当前缺陷统计

| 优先级 | 累计发现 | 已修复 | 遗留 | 修复率 |
|--------|----------|--------|------|--------|
| P0 | 3 | 3 | 0 | 100% |
| P1 | 8 | 8 | 0 | 100% |
| P2 | 12 | 11 | 1 | 91.7% |
| P3 | 6 | 4 | 2 | 66.7% |
| **合计** | **29** | **26** | **3** | **89.7%** |

### 12.4 遗留缺陷清单

| 缺陷编号 | 优先级 | 描述 | 影响范围 | 规避方案 |
|----------|--------|------|----------|----------|
| BUG-P2-012 | P2 | Vite 构建产物 chunk 超过 500KB 警告 | 首屏加载稍慢（仍满足 3s 指标） | 后续版本拆分按需加载 |
| BUG-P3-005 | P3 | 仿真页面在 1024px 宽度下左侧面板略挤 | 小屏用户体验 | 建议使用 ≥1280px 分辨率 |
| BUG-P3-006 | P3 | Agent 日志页时间戳未本地化显示 | 时间显示为 UTC | 不影响功能，后续优化 |

---

## 十三、测试总结与风险评估

### 13.1 测试覆盖率统计

| 维度 | 覆盖情况 | 备注 |
|------|----------|------|
| 单元测试用例数 | 59 条 | 全部通过 |
| 集成测试用例数 | 12 条 | 全部通过 |
| 系统测试用例数 | 12 条 | 全部通过 |
| 性能测试指标 | 12 项 | 全部达标 |
| 安全测试用例数 | 12 条 | 全部通过 |
| 后端 Agent 模块覆盖 | 9/9（100%） | 含诊断/资源/路径/画像/检索/编排/仿真/挑战/信任 |
| 前端页面覆盖 | 7/7（100%） | 含对话/画像/路径/资源/仿真/日志/设置 |
| API 接口覆盖 | 15/15（100%） | Swagger UI 全量验证 |

### 13.2 质量评估结论

| 评估项 | 结论 |
|--------|------|
| 功能完整性 | 8 Agent 协同、三层诊断、5 类资源、7 场景仿真、可解释路径规划均已实现并通过验证 |
| 可靠性 | LLM 超时自动降级 Mock、向量库兑底、诊断 JSON 类型安全、资源生成兑底，核心链路无单点故障 |
| 性能达标 | 所有性能指标均满足目标值，10 并发下服务稳定无降级 |
| 安全合规 | 越界拦截有效、密钥不泄露、注入防护到位、会话隔离正常 |
| 可维护性 | 代码规范（全模块 docstring）、依赖清洁、测试自动化（pytest 一键执行） |

### 13.3 遗留风险评估

| 风险项 | 影响级别 | 描述 | 缓解措施 |
|--------|----------|------|----------|
| LLM 服务不可用 | 中 | 讯飞星火 API 可能因网络或配额问题不可访问 | Mock 模式自动降级，演示不受影响 |
| 向量库数据量不足 | 低 | 当前仅覆盖计算机网络核心章节 | _FALLBACK_DOCS 兜底，确保检索不返空 |
| 前端构建 chunk 较大 | 低 | 首屏加载存在优化空间 | 已满足 3s 指标，后续版本按需拆分 |
| SQLite 并发限制 | 低 | SQLite 不支持高并发写入 | 当前场景（演示/评审）并发量有限，不构成瓶颈 |

### 13.4 测试结论

本项目已完成五个维度（单元/集成/系统/性能/安全）的全面测试验证：

- **自动化测试**：59 条单元测试全部通过，覆盖核心 Agent 逻辑、数据模型、LLM 客户端
- **接口测试**：12 条集成测试全部通过，验证 API 层协议与 Agent 协同链路
- **端到端测试**：12 条系统测试全部通过，覆盖用户完整操作流程
- **非功能测试**：12 项性能指标全部达标，12 条安全用例全部通过
- **缺陷管理**：累计 29 个缺陷，修复率 89.7%，P0/P1 全部清零，遗留 3 个 P2/P3 低优先级问题

**最终结论：系统质量满足软件杯 A3 赛项提交标准，具备上线演示条件。**
